% ============================================================
%  HW02 – Domain Testing on EShop | Report Template
% ============================================================
\documentclass[12pt, a4paper]{report}

% ---------- Encoding & Language ----------
\usepackage[utf8]{inputenc}
\usepackage[T5]{fontenc}
\usepackage[vietnamese, english]{babel}

% ---------- Page Layout ----------
\usepackage[
    top=2.5cm, bottom=2.5cm,
    left=3cm,  right=2.5cm
]{geometry}

% ---------- Typography ----------
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{setspace}
\onehalfspacing

% ---------- Colors (đen trắng / grayscale only) ----------
\usepackage[dvipsnames, table]{xcolor}
% Toàn bộ báo cáo dùng thuần đen trắng: mọi màu sắc được ép về đen/trắng/xám.
\definecolor{primaryblue}{gray}{0}          % tiêu đề, link, caption -> đen
\definecolor{headerblue}{gray}{0}
\definecolor{lightgray}{gray}{0.95}         % nền code -> xám rất nhạt
\definecolor{pastelgreen}{gray}{1}          % nền box -> trắng
\definecolor{pastelred}{gray}{1}
\definecolor{pastelyellow}{gray}{1}
% Ép các màu chuẩn (dùng trong \textcolor Pass/Fail/Note, code) về đen:
\definecolor{green}{gray}{0}
\definecolor{red}{gray}{0}
\definecolor{orange}{gray}{0}
\definecolor{BrickRed}{gray}{0}

% ---------- Math ----------
\usepackage{amsmath, amssymb}

% ---------- Tables ----------
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{array}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}p{#1}}

% ---------- Graphics ----------
\usepackage{graphicx}
\usepackage{float}
\graphicspath{{./figures/}}

% ---------- Listings (Code) ----------
\usepackage{listings}
\lstset{
    basicstyle=\ttfamily\small,
    keywordstyle=\color{primaryblue}\bfseries,
    commentstyle=\color{gray}\itshape,
    stringstyle=\color{BrickRed},
    backgroundcolor=\color{lightgray},
    frame=single,
    rulecolor=\color{gray!40},
    breaklines=true,
    showstringspaces=false,
    numbers=left,
    numberstyle=\tiny\color{gray},
    tabsize=4,
    captionpos=b
}

% ---------- Hyperlinks ----------
\usepackage[
    colorlinks=true,
    linkcolor=primaryblue,
    urlcolor=primaryblue,
    citecolor=primaryblue,
    pdftitle={HW02 – Domain Testing on EShop},
    pdfauthor={Lý Quốc Thạnh},
    pdfsubject={Software Testing}
]{hyperref}

% ---------- Headers & Footers ----------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\textcolor{gray}{\subjectName}}
\fancyhead[R]{\small\textcolor{gray}{\hwTitle}}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

% ---------- Section Styling ----------
\usepackage{titlesec}
\titleformat{\chapter}[block]
    {\normalfont\LARGE\bfseries\color{primaryblue}}
    {\thechapter.}{1em}{}
\titleformat{\section}
    {\normalfont\Large\bfseries\color{primaryblue!80}}
    {\thesection}{1em}{}
\titleformat{\subsection}
    {\normalfont\large\bfseries}
    {\thesubsection}{1em}{}
\titleformat{\subsubsection}
    {\normalfont\normalsize\bfseries}
    {\thesubsubsection}{1em}{}

% ---------- Captions ----------
\usepackage{caption}
\captionsetup{
    font=small,
    labelfont={bf, color=primaryblue},
    skip=6pt
}

% ---------- Enumitem ----------
\usepackage{enumitem}
\setlist[itemize]{leftmargin=*, topsep=4pt, itemsep=2pt}
\setlist[enumerate]{leftmargin=*, topsep=4pt, itemsep=2pt}

% ---------- Boxes & Notes ----------
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable}
\newtcolorbox{notebox}[1][]{
    colback=white, colframe=black, coltitle=black, colbacktitle=lightgray,
    title={\textbf{Note}}, fonttitle=\bfseries,
    #1
}
\newtcolorbox{bugbox}[1][]{
    colback=white, colframe=black, coltitle=black, colbacktitle=lightgray,
    title={\textbf{Bug}}, fonttitle=\bfseries,
    #1
}
\newtcolorbox{passbox}[1][]{
    colback=white, colframe=black, coltitle=black, colbacktitle=lightgray,
    title={\textbf{Pass}}, fonttitle=\bfseries,
    #1
}

% ---------- Metadata ----------
\newcommand{\subjectName}{KIỂM CHỨNG PHẦN MỀM}
\newcommand{\hwTitle}{HW02 – Domain Testing on EShop}
\newcommand{\instructor}{%
    \begin{tabular}[t]{@{}l@{}}
        Lâm Quang Vũ \\
        Trương Phước Lộc \\
        Hồ Tuấn Thanh
    \end{tabular}%
}
\newcommand{\studentName}{Lý Quốc Thạnh}
\newcommand{\studentID}{23127262}
\newcommand{\studentEmail}{lqthanh23@clc.fitus.edu.vn}

% ============================================================
\begin{document}

% ===== TRANG BÌA =====
\begin{titlepage}
    \centering
    \vspace*{1cm}
    \textbf{\Large TRƯỜNG ĐẠI HỌC KHOA HỌC TỰ NHIÊN}\\
    \textbf{\large KHOA CÔNG NGHỆ THÔNG TIN}\\
    \vspace{3cm}
    {\LARGE \textbf{\subjectName}}\\[2cm]
    {\LARGE \textbf{\hwTitle}}\\[2cm]
    \vspace{1.5cm}
    \begin{table}[h]
        \centering
        \large
        \begin{tabular}{ll}
            \textbf{Giảng viên:}  & \instructor \\[0.5cm]
            \textbf{Sinh viên:}   & \studentName \\
            \textbf{MSSV:}        & \studentID \\
            \textbf{Email:}       & \studentEmail \\
        \end{tabular}
    \end{table}
    \vfill
    {\large TP. Hồ Chí Minh, \today}
\end{titlepage}

% ===== MỤC LỤC =====
\tableofcontents
\listoffigures
\listoftables
\newpage

% ============================================================
%  CHƯƠNG 1: TỔNG QUAN
% ============================================================
\chapter{Tổng Quan}

\section{Các Tính Năng Được Chọn}
\begin{table}[H]
    \centering
    \caption{Tính năng được chọn}
    \begin{tabularx}{\linewidth}{C{1.5cm} C{2cm} L{4cm} L{6cm}}
        \toprule
        \textbf{Pool} & \textbf{FR-ID} & \textbf{Tên tính năng} & \textbf{Mô tả} \\
        \midrule
        A & FR-01 & Đăng ký tài khoản & Tạo tài khoản mới với họ tên, email, mật khẩu. \\
        B & FR-09 & Mã giảm giá & Áp dụng mã giảm giá (percent/fixed) cho đơn hàng. \\
        C & FR-14 & Quản lý danh mục (CRUD) & Thêm/sửa/xóa danh mục sản phẩm (trang admin). \\
        D & FR-07 & Giỏ hàng (Mobile) & Thêm/sửa số lượng/xóa sản phẩm trong giỏ trên app mobile. \\
        \bottomrule
    \end{tabularx}
\end{table}

% ============================================================
%  CHƯƠNG 2: FEATURE A — POOL A
% ============================================================
\chapter{Feature A — Đăng ký tài khoản (Pool A)}

\section{Mô Tả Tính Năng}
Tính năng \textbf{FR-01 — Đăng ký tài khoản} cho phép người dùng tạo tài khoản mới bằng
\emph{họ tên}, \emph{email} và \emph{mật khẩu}. Nó được cài đặt qua hai tầng:

\begin{itemize}
    \item \textbf{Tầng giao diện (UI):} \texttt{frontend-web/src/pages/Register.jsx} — biểu mẫu
    với ba trường. Đây là nơi \emph{duy nhất} có kiểm tra dữ liệu: một biểu thức chính quy
    (regex) kiểm tra ``mật khẩu mạnh'' phía client. Khi hợp lệ, nó gửi \texttt{POST} tới API
    rồi chuyển sang trang \texttt{/login}.
    \item \textbf{Tầng API:} \texttt{POST /api/register} (\texttt{backend/server.js}) — chèn
    thẳng \verb|{name, email, password}| vào bảng \texttt{users}, \emph{không} kiểm tra hợp lệ,
    \emph{không} kiểm tra trùng lặp, và lưu mật khẩu ở dạng \textbf{văn bản thuần (plaintext)}.
\end{itemize}

\noindent\textbf{Đầu vào (input):} \texttt{name} (string), \texttt{email} (string),
\texttt{password} (string).\\
\textbf{Đầu ra (output):} mã HTTP (\texttt{200}/\texttt{500}), body JSON
(\verb|{message, id}| hoặc \verb|{error}|), thông báo lỗi phía client, bản ghi được lưu trong
DB (kèm mật khẩu plaintext, \texttt{role='user'}), và điều hướng sang \texttt{/login}.

\vspace{4pt}
\noindent\textbf{Ràng buộc mật khẩu theo đặc tả} (chữ trợ giúp trong \texttt{Register.jsx}):
\emph{``Tối thiểu 8 ký tự, có chữ hoa, chữ thường, số và ký tự đặc biệt.''}
Tuy nhiên regex thực tế được cài đặt là:

\begin{lstlisting}[caption={Regex kiểm tra mật khẩu (Register.jsx)},numbers=none]
/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*\s)[A-Za-z\d\s]{8,}$/
\end{lstlisting}

\begin{notebox}
Regex này \textbf{bắt buộc có khoảng trắng} (\texttt{(?=.*\textbackslash s)}) và, do lớp ký tự
là \texttt{[A-Za-z\textbackslash d\textbackslash s]}, nó \textbf{cấm mọi ký tự đặc biệt} — ngược
hoàn toàn với đặc tả (vốn yêu cầu \emph{phải có} ký tự đặc biệt). Đây là khiếm khuyết trọng tâm
mà chương này nhắm tới. Hệ quả: mật khẩu mạnh như \texttt{Password123!} bị \emph{từ chối}, còn
mật khẩu yếu như \texttt{Passw0rd 1} lại được \emph{chấp nhận}.
\end{notebox}

\begin{notebox}
\textbf{Quy ước cột kết quả.} Cột \emph{Kết quả}/\emph{Trạng thái} là kết quả \textbf{thực thi
thật} (2026-07-02): gọi trực tiếp API \texttt{POST /api/register}, \texttt{GET /api/users/me}
trên backend EShop chạy tại \texttt{localhost:3000}, và chạy đúng regex mật khẩu trích từ
\texttt{Register.jsx}. Mọi kết quả khớp với dự đoán từ mã nguồn ban đầu. Cột
\emph{Expected} là hành vi \emph{đúng theo đặc tả}. Ca nào có Kết quả khác Expected là lỗi
(bug) đã được xác nhận.
\end{notebox}

\section{Domain Testing}

Domain Testing được áp dụng theo đúng trình tự trong bài giảng: \emph{(1) xác định biến $\to$
(2) phân hoạch miền thành lớp tương đương $\to$ (3) chọn điểm kiểm thử ON/OFF/IN/OUT trên biên
$\to$ (4) thiết kế ca}. Nguyên tắc cốt lõi: coi mỗi biến đầu vào là một \emph{miền} giá trị,
chia miền thành các lớp cho \emph{cùng một hành vi}, rồi chỉ cần một điểm đại diện cho mỗi lớp
cộng với các điểm sát biên --- nhờ đó giảm số ca mà vẫn phủ hết hành vi. Với FR-01, biến trọng
tâm là \texttt{password} (nhiều lớp ký tự) và \texttt{email} (định dạng + tính duy nhất); biến
có thứ tự duy nhất để làm biên số học là \texttt{password.length}. Bước~5 tổng hợp ma trận truy
vết để chứng minh mọi lớp tương đương đều có ít nhất một ca kiểm thử.

\subsection{Bước 1: Xác Định Các Biến Đầu Vào}
Liệt kê mọi biến đầu vào (kể cả biến ẩn: độ dài mật khẩu, các lớp ký tự, tính duy nhất của
email).

\begin{table}[H]
    \centering
    \caption{Danh sách biến đầu vào — Feature A (FR-01)}
    \begin{tabularx}{\linewidth}{C{0.5cm} L{3cm} L{2.4cm} L{3.2cm} X}
        \toprule
        \textbf{\#} & \textbf{Biến} & \textbf{Kiểu dữ liệu} & \textbf{Ràng buộc} & \textbf{Ghi chú} \\
        \midrule
        1 & name & string (TEXT) & UI \texttt{required} & Không giới hạn độ dài/định dạng; backend không kiểm. \\
        2 & email & string (TEXT) & UI \texttt{required} & \texttt{type="text"} nên không kiểm định dạng; cột \emph{không} \texttt{UNIQUE}. \\
        3 & password & string (TEXT) & regex client & Backend không kiểm; lưu plaintext. \\
        4 & password.length & integer (ẩn) & $\ge 8$ (regex \texttt{\{8,\}}) & Biến có thứ tự $\Rightarrow$ dùng cho BVA. \\
        5 & has\_lower / has\_upper / has\_digit & boolean (ẩn) & phải có mỗi loại & Điều kiện lớp ký tự của regex. \\
        6 & has\_whitespace & boolean (ẩn) & regex \emph{bắt buộc} & Khiếm khuyết (đặc tả không yêu cầu). \\
        7 & has\_special\_char & boolean (ẩn) & regex \emph{cấm} & Khiếm khuyết (đặc tả \emph{yêu cầu}). \\
        8 & email.isUnique & boolean (ẩn) & phải duy nhất & Không nơi nào kiểm tra. \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Bước 2: Phân Hoạch Miền (Domain Partitioning)}
Mỗi biến được chia thành các lớp tương đương hợp lệ/không hợp lệ (có mã EC). Dấu \emph{*}
đánh dấu lớp mà một hệ thống \emph{đúng} phải xử lý theo đặc tả nhưng mã hiện tại làm sai.

\subsubsection{Biến name}
\begin{table}[H]
    \centering
    \caption{Phân hoạch miền — name}
    \begin{tabularx}{\linewidth}{C{1.4cm} L{6cm} C{1.6cm} X}
        \toprule
        \textbf{EC} & \textbf{Điều kiện} & \textbf{Loại} & \textbf{Ví dụ} \\
        \midrule
        EC-N1 & Tên thường, không rỗng & Valid & \texttt{Nguyen Van A} \\
        EC-N2 & Rỗng & Invalid & \verb|""| \\
        EC-N3 & Chỉ khoảng trắng & Invalid & \verb|"   "| \\
        EC-N4 & Rất dài (không có max) & Valid* & 100.000 ký tự \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsubsection{Biến email}
\begin{table}[H]
    \centering
    \caption{Phân hoạch miền — email}
    \begin{tabularx}{\linewidth}{C{1.4cm} L{6cm} C{1.6cm} X}
        \toprule
        \textbf{EC} & \textbf{Điều kiện} & \textbf{Loại} & \textbf{Ví dụ} \\
        \midrule
        EC-E1 & Đúng định dạng, chưa dùng & Valid & \texttt{new@domain.com} \\
        EC-E2 & Sai định dạng & Invalid & \texttt{notanemail} \\
        EC-E3 & Rỗng & Invalid & \verb|""| \\
        EC-E4 & Trùng email đã tồn tại & Invalid & \texttt{test@eshop.com} \\
        EC-E5 & Có khoảng trắng bao quanh & Invalid & \verb|" test@eshop.com "| \\
        EC-E6 & Payload dạng SQL-injection & Invalid & \texttt{x'; DROP TABLE users;-{}-} \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsubsection{Biến password}
\begin{table}[H]
    \centering
    \caption{Phân hoạch miền — password}
    \begin{tabularx}{\linewidth}{C{1.4cm} L{6cm} C{1.6cm} X}
        \toprule
        \textbf{EC} & \textbf{Điều kiện} & \textbf{Loại} & \textbf{Ví dụ} \\
        \midrule
        EC-P1 & Qua được regex (có ws, không ký tự đặc biệt, $\ge 8$) & Valid* & \texttt{Passw0rd 1} \\
        EC-P2 & Mạnh đúng đặc tả (hoa+thường+số+đặc biệt, không ws) & Spec-valid & \texttt{Password123!} \\
        EC-P3 & Thiếu chữ thường & Invalid & \texttt{PASSW0RD 1} \\
        EC-P4 & Thiếu chữ hoa & Invalid & \texttt{passw0rd 1} \\
        EC-P5 & Thiếu chữ số & Invalid & \texttt{Password x} \\
        EC-P6 & Thiếu khoảng trắng (điều kiện lỗi) & Invalid & \texttt{Password1} \\
        EC-P7 & Có ký tự đặc biệt & Invalid & \texttt{Passw0rd !} \\
        EC-P8 & Độ dài $<8$ & Invalid & \texttt{Aa1 bcd} \\
        EC-P9 & Rỗng & Invalid & \verb|""| \\
        \bottomrule
    \end{tabularx}
\end{table}

\noindent\emph{Spec-valid} = đúng đặc tả nhưng bị mã \emph{từ chối}; \emph{Valid*} = mã
\emph{chấp nhận} dù sai đặc tả. Hai lớp EC-P1 và EC-P2 chính là nơi lỗi biểu hiện rõ nhất.

\subsection{Bước 3: Chọn Điểm Kiểm Thử (Test Point Selection)}
Biến có thứ tự duy nhất là \texttt{password.length} với ranh giới $\ge 8$. Áp dụng chiến lược
ON/OFF/IN, mọi giá trị dưới đây giữ nguyên các điều kiện lớp ký tự còn lại (1 chữ thường, 1
chữ hoa, 1 chữ số, 1 khoảng trắng) để độ dài là biến duy nhất thay đổi.

\begin{table}[H]
    \centering
    \caption{Điểm ON/OFF/IN/OUT — password.length ($\ge 8$)}
    \begin{tabularx}{\linewidth}{L{2.4cm} L{3.2cm} C{2cm} X}
        \toprule
        \textbf{Loại điểm} & \textbf{Giá trị} & \textbf{Độ dài} & \textbf{Kỳ vọng} \\
        \midrule
        ON-point  & \texttt{Aa1 bcde}     & 8  & Chấp nhận (đúng biên) \\
        OFF-point & \texttt{Aa1 bcd}      & 7  & Từ chối (dưới biên) \\
        IN-point  & \texttt{Aa1 bcdefgh}  & 11 & Chấp nhận (trong miền) \\
        OUT-point & \verb|""|             & 0  & Từ chối (ngoài miền) \\
        \bottomrule
    \end{tabularx}
\end{table}

\noindent\textbf{Nhận xét quan trọng:} biên độ dài (\texttt{\{8,\}}) được cài đặt \emph{đúng}
— không có off-by-one tại 8. Các khiếm khuyết nằm ở \emph{điều kiện lớp ký tự} (bắt buộc
khoảng trắng, cấm ký tự đặc biệt), là các \emph{phân hoạch} (Bước~2) chứ không phải biên số
học. Vì vậy Domain Testing phát hiện nhiều lỗi hơn BVA thuần túy ở tính năng này.

\subsection{Bước 4: Thiết Kế Test Cases — Domain Testing}
\begin{longtable}{C{1cm} L{3cm} L{3.5cm} L{3cm} C{2cm} C{1.5cm}}
    \caption{Test cases — Domain Testing, Feature A} \\
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endfirsthead
    \multicolumn{6}{c}{\small\itshape (tiếp theo)} \\
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endhead
    \midrule
    \multicolumn{6}{r}{\small\itshape (còn tiếp)} \\
    \endfoot
    \bottomrule
    \endlastfoot
    TC-A-DT-01 & MK mạnh có ký tự đặc biệt, đúng đặc tả \newline (BUG-A-01) & pw=\texttt{Password123!} & Đăng ký OK, sang \texttt{/login} & Báo ``MK quá yếu'' & \textcolor{red}{Fail} \\
    TC-A-DT-02 & MK qua regex: có ws, không ký tự đặc biệt \newline (BUG-A-02) & pw=\texttt{Passw0rd 1} & Từ chối (thiếu ký tự đặc biệt) & Đăng ký OK & \textcolor{red}{Fail} \\
    TC-A-DT-03 & Thiếu chữ hoa & pw=\texttt{passw0rd 1} & Từ chối & Từ chối & \textcolor{green!60!black}{Pass} \\
    TC-A-DT-04 & Thiếu chữ số & pw=\texttt{Password x} & Từ chối & Từ chối & \textcolor{green!60!black}{Pass} \\
    TC-A-DT-05 & Có ký tự đặc biệt kèm ws \newline (BUG-A-01) & pw=\texttt{Passw0rd !} & Chấp nhận (MK mạnh) & Từ chối (cấm `!') & \textcolor{red}{Fail} \\
    TC-A-DT-06 & Email sai định dạng \newline (BUG-A-03) & email=\texttt{notanemail} & Từ chối email không hợp lệ & Đăng ký OK & \textcolor{red}{Fail} \\
    TC-A-DT-07 & Email trùng đã tồn tại \newline (BUG-A-04) & email=\texttt{test@eshop.com} & Từ chối trùng & Tạo bản ghi thứ 2 & \textcolor{red}{Fail} \\
    TC-A-DT-08 & (API) Body rỗng \newline (BUG-A-05) & \texttt{POST \{\}} & 400 từ chối & 200, user toàn NULL & \textcolor{red}{Fail} \\
    TC-A-DT-09 & (API) MK rất yếu \newline (BUG-A-05) & pw=\texttt{"1"} & Từ chối & 200 registered & \textcolor{red}{Fail} \\
    TC-A-DT-10 & (API) Payload injection ở email & email=\texttt{x'; DROP TABLE users;-{}-} & Chặn/thoát an toàn & Lưu literal, 200 (tham số hóa) & \textcolor{green!60!black}{Pass} \\
    TC-A-DT-11 & Tên chỉ khoảng trắng & name=\verb|"   "| & Từ chối (rỗng ngữ nghĩa) & \texttt{required} cho qua, OK & \textcolor{red}{Fail} \\
    TC-A-DT-12 & (API) Cách lưu mật khẩu \newline (BUG-A-06) & Đăng ký rồi \texttt{GET /users/me} & MK đã băm, không trả về & Trả về MK plaintext & \textcolor{red}{Fail} \\
\end{longtable}

\subsection{Bước 5: Ma Trận Truy Vết Độ Phủ (EC $\to$ Test Case)}
Bảng dưới đối chiếu \emph{mọi} lớp tương đương ở Bước~2 với ca kiểm thử phủ nó (kể cả ca BVA),
chứng minh tiêu chí ``mỗi lớp $\ge 1$ điểm đại diện''. Một số lớp cùng \emph{họ hành vi} dùng
chung một ca đại diện theo đúng nguyên tắc phân hoạch tương đương.

\begin{table}[H]
    \centering
    \caption{Truy vết EC $\to$ TC — Feature A (FR-01)}
    \begin{tabularx}{\linewidth}{C{1.8cm} C{3.4cm} X}
        \toprule
        \textbf{EC} & \textbf{Ca phủ (DT/BVA)} & \textbf{Ghi chú} \\
        \midrule
        EC-N1 & (nền ca hợp lệ) & Tên hợp lệ dùng ở TC-A-DT-01/02. \\
        EC-N2 & TC-A-DT-08 & Body rỗng $\Rightarrow$ name rỗng. \\
        EC-N3 & TC-A-DT-11 & Tên chỉ khoảng trắng. \\
        EC-N4 & TC-A-BV-06 & Biên trên độ dài (BVA). \\
        EC-E1 & (nền ca hợp lệ) & Email hợp lệ dùng ở TC-A-DT-01/02. \\
        EC-E2 & TC-A-DT-06 & Sai định dạng. \\
        EC-E3 & TC-A-DT-08 & Body rỗng $\Rightarrow$ email rỗng. \\
        EC-E4 & TC-A-DT-07 & Email trùng. \\
        EC-E5 & TC-A-DT-07 (đại diện) & Biến thể khoảng trắng của lỗi định danh trùng; chi tiết ở Gap Analysis. \\
        EC-E6 & TC-A-DT-10 & Payload injection. \\
        EC-P1 & TC-A-DT-02, BV-02 & Qua regex (ws, không đặc biệt, $\ge 8$). \\
        EC-P2 & TC-A-DT-01 & Mạnh đúng đặc tả. \\
        EC-P3 & TC-A-DT-03/04 (đại diện) & Cùng họ ``thiếu một lớp ký tự bắt buộc''; regex đối xứng nên một đại diện đủ. \\
        EC-P4 & TC-A-DT-03 & Thiếu chữ hoa. \\
        EC-P5 & TC-A-DT-04 & Thiếu chữ số. \\
        EC-P6 & TC-A-DT-01 & \texttt{Password123!} không có khoảng trắng $\Rightarrow$ bị từ chối (chính điều kiện EC-P6). \\
        EC-P7 & TC-A-DT-05 & Có ký tự đặc biệt. \\
        EC-P8 & TC-A-DT-09, BV-01 & Độ dài $<8$. \\
        EC-P9 & TC-A-DT-08, BV-04 & Rỗng. \\
        \bottomrule
    \end{tabularx}
\end{table}
\noindent\textbf{Kết luận độ phủ:} 4/4 lớp \texttt{name}, 6/6 lớp \texttt{email}, 9/9 lớp
\texttt{password} đều có ít nhất một ca kiểm thử.

\section{Boundary Value Analysis (BVA)}

Boundary Value Analysis bổ trợ Domain Testing bằng cách tập trung vào \emph{vùng sát ranh giới}
--- nơi lỗi hay xuất hiện nhất. Quy trình theo bài giảng: \emph{(1) xác định mọi biên (số học /
độ dài); (2) với mỗi biên sinh bộ ba giá trị} \texttt{off (biên$-1$), on (biên), on$+1$}, cộng
điểm ngoài miền khi cần. Với FR-01, biên số học duy nhất là \texttt{password.length}$\,\ge 8$;
\texttt{name}/\texttt{email} chỉ có biên ``khác rỗng'' (một điểm: rỗng vs không rỗng) và
\emph{không} có biên trên --- bản thân việc thiếu biên trên là một phát hiện. Bước~3 tổng hợp ma
trận phủ biên để chứng minh mỗi biên đều đủ điểm off/on/on$+1$.

\subsection{Bước 1: Xác Định Ranh Giới (Boundaries)}
Chỉ có \texttt{password.length} là biên số học được xác định rõ ($\ge 8$). \texttt{name} và
\texttt{email} chỉ có biên dưới ``khác rỗng'' (do UI \texttt{required}); \emph{không} có biên
trên nào được đặc tả hay cưỡng chế (SQLite \texttt{TEXT} không giới hạn) — bản thân điều này
là một phát hiện (xem Gap Analysis).

\begin{table}[H]
    \centering
    \caption{Bảng giá trị biên — Feature A (FR-01)}
    \begin{tabularx}{\linewidth}{L{3cm} C{1.6cm} C{1.6cm} C{1.8cm} C{2.4cm} X}
        \toprule
        \textbf{Biến} & \textbf{Min} & \textbf{Min+1} & \textbf{Max-1} & \textbf{Max} & \textbf{Ghi chú} \\
        \midrule
        password.length & 8 & 9 & --- & --- (không có max) & Biên dưới đúng; off-point $=7$; không có biên trên. \\
        name.length     & 1 & 2 & --- & --- (không có max) & Chỉ \texttt{required} chặn rỗng ở UI. \\
        email.length    & 1 & 2 & --- & --- (không có max) & Tương tự; backend nhận cả \texttt{NULL}. \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Bước 2: Thiết Kế Test Cases — BVA}
\begin{longtable}{C{1cm} L{3cm} L{3.5cm} L{3cm} C{2cm} C{1.5cm}}
    \caption{Test cases — BVA, Feature A} \\
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endfirsthead
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endhead
    \bottomrule
    \endlastfoot
    TC-A-BV-01 & Min$-1$: độ dài 7 & pw=\texttt{Aa1 bcd} (7) & Từ chối (dưới biên) & Từ chối & \textcolor{green!60!black}{Pass} \\
    TC-A-BV-02 & Min: độ dài 8 & pw=\texttt{Aa1 bcde} (8) & Chấp nhận (qua regex) & Chấp nhận & \textcolor{green!60!black}{Pass} \\
    TC-A-BV-03 & Min$+1$: độ dài 9 & pw=\texttt{Aa1 bcdef} (9) & Chấp nhận & Chấp nhận & \textcolor{green!60!black}{Pass} \\
    TC-A-BV-04 & Ngoài biên dưới: rỗng & pw=\verb|""| (0) & Từ chối & Từ chối (\texttt{required}+regex) & \textcolor{green!60!black}{Pass} \\
    TC-A-BV-05 & Biên trên không xác định (API) \newline (BUG-A-05) & pw dài 100.000 ký tự & Nên giới hạn/từ chối & Chấp nhận, lưu toàn bộ & \textcolor{red}{Fail} \\
    TC-A-BV-06 & (API) name/email cực dài & name dài 100.000 ký tự & Nên giới hạn/từ chối & Chấp nhận & \textcolor{red}{Fail} \\
\end{longtable}

\subsection{Bước 3: Ma Trận Phủ Biên (off / on / on$+1$)}
Bảng dưới chứng minh mỗi biên ở Bước~1 đều được kiểm đủ ba giá trị (kèm điểm ngoài miền), lấy ca
từ cả bảng BVA lẫn Domain Testing.

\begin{table}[H]
    \centering
    \caption{Phủ biên — Feature A (FR-01)}
    \begin{tabularx}{\linewidth}{L{2.8cm} C{1.7cm} C{1.7cm} C{1.7cm} X}
        \toprule
        \textbf{Biên} & \textbf{Off} & \textbf{On} & \textbf{On$+1$} & \textbf{Ngoài / Ghi chú} \\
        \midrule
        password.length $\ge 8$ & BV-01 (7) & BV-02 (8) & BV-03 (9) & Ngoài miền: BV-04 (0). Không có max $\Rightarrow$ robustness BV-05 (100k). \\
        name.length $\ge 1$ & DT-08/11 (0) & ca hợp lệ ($\ge 1$) & --- & Biên đơn điểm (rỗng/không rỗng); không có max $\Rightarrow$ BV-06 (100k). \\
        email.length $\ge 1$ & DT-08 (0) & ca hợp lệ ($\ge 1$) & --- & Tương tự; không có max $\Rightarrow$ BV-06. \\
        \bottomrule
    \end{tabularx}
\end{table}
\noindent\textbf{Kết luận:} \texttt{password.length} phủ đủ off/on/on$+1$ + ngoài miền;
\texttt{name}/\texttt{email} là biên \emph{đơn điểm} (rỗng vs không rỗng) nên không có on$+1$ số
học --- phát hiện chính là \emph{thiếu biên trên}, đã kiểm ở BV-05/06.

\section{AI Gap Analysis}
Sau khi AI sinh bộ ca kiểm thử theo phân hoạch/biên, ta rà soát lại để tìm những ca dễ bị bỏ
sót. Lý do bỏ sót được phân loại: \emph{chất lượng prompt}, \emph{giới hạn công cụ AI}, hoặc
\emph{độ phức tạp nội tại của tính năng}.

\begin{table}[H]
    \centering
    \caption{AI Gap Analysis — Feature A (FR-01)}
    \begin{tabularx}{\linewidth}{C{0.5cm} X L{4cm} C{1.8cm}}
        \toprule
        \textbf{\#} & \textbf{Ca/bug bị bỏ sót} & \textbf{Lý do bỏ sót} & \textbf{Bổ sung ở TC} \\
        \midrule
        1 & Backend hoàn toàn không kiểm tra dữ liệu — prompt chỉ nhìn regex phía UI sẽ bỏ sót lỗ hổng khi gọi API trực tiếp. & Chất lượng prompt (chỉ tập trung tầng UI) + độ phức tạp (validation tách 2 tầng). & TC-A-DT-08/09 \\
        2 & Mật khẩu lưu plaintext và lộ qua \texttt{GET /users/me} — đây là biến \emph{đầu ra}, không phải input. & Prompt tập trung input; giới hạn AI: không tự kiểm khía cạnh bảo mật. & TC-A-DT-12 \\
        3 & Email trùng phá vỡ định danh khi đăng nhập (tương tác đa tính năng/trạng thái). & Độ phức tạp nội tại (cross-feature register$\to$login). & TC-A-DT-07 \\
        4 & Đảo ngược điều kiện regex: \emph{bắt buộc khoảng trắng} và \emph{cấm ký tự đặc biệt}. Đọc chữ trợ giúp dễ giả định ngược lại. & Giới hạn công cụ + chất lượng prompt (không đọc kỹ regex thực thi). & TC-A-DT-01/02/05 \\
        5 & Email có khoảng trắng đầu/cuối tạo bản ghi ``trùng nhưng khác'' (không chuẩn hóa). & Độ phức tạp (chuẩn hóa/locale). & EC-E5 (bổ sung) \\
        6 & Không có giới hạn độ dài trên cho mọi trường (biên trên không xác định). & Độ phức tạp/robustness; BVA thường chỉ nhìn biên đã đặc tả. & TC-A-BV-05/06 \\
        \bottomrule
    \end{tabularx}
\end{table}

\section{Bug Reports — Feature A}
Sáu lỗi ứng viên dưới đây được suy ra từ phân tích mã nguồn. Cần \textbf{xác nhận bằng thực
thi}, chụp màn hình (đặt tại \texttt{figures/} hoặc \texttt{screenshots/}) và tạo GitHub Issue
(điền URL vào ô tương ứng) trước khi nộp.
 
\subsection{BUG-A-01: Mật khẩu mạnh (có ký tự đặc biệt) bị từ chối}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-A-01 \\
        Tính năng    & FR-01 \quad\textbf{Kỹ thuật:} Domain + BVA \\
        Mức độ       & Major \\
        Mô tả        & Regex client bắt buộc có khoảng trắng và cấm ký tự đặc biệt; mật khẩu mạnh đúng đặc tả bị từ chối. \\
        Tái hiện     & 1. Mở trang Đăng ký. \newline 2. Nhập name/email hợp lệ. \newline 3. Nhập mật khẩu \texttt{Password123!}. \newline 4. Bấm ``Đăng Ký''. \\
        Expected     & Đăng ký thành công, chuyển tới \texttt{/login}. \\
        Actual       & Báo lỗi ``Mật khẩu quá yếu!'' (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_a_01.png}\caption{Screenshot — BUG-A-01}\end{figure}

\subsection{BUG-A-02: Mật khẩu yếu (không có ký tự đặc biệt) được chấp nhận}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-A-02 \\
        Tính năng    & FR-01 \quad\textbf{Kỹ thuật:} Domain \\
        Mức độ       & Major \\
        Mô tả        & Mật khẩu chỉ cần có khoảng trắng, không có ký tự đặc biệt vẫn qua regex — trái đặc tả. \\
        Tái hiện     & 1. Mở trang Đăng ký. \newline 2. Nhập name/email hợp lệ. \newline 3. Nhập mật khẩu \texttt{Passw0rd 1}. \newline 4. Bấm ``Đăng Ký''. \\
        Expected     & Từ chối (thiếu ký tự đặc biệt). \\
        Actual       & Đăng ký thành công (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_a_02.png}\caption{Screenshot — BUG-A-02}\end{figure}

\subsection{BUG-A-03: Email sai định dạng được chấp nhận}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-A-03 \\
        Tính năng    & FR-01 \quad\textbf{Kỹ thuật:} Domain \\
        Mức độ       & Major \\
        Mô tả        & Trường email dùng \texttt{type="text"}, không có kiểm tra định dạng ở UI lẫn backend. \\
        Tái hiện     & 1. Mở trang Đăng ký. \newline 2. Nhập email \texttt{notanemail}, mật khẩu \texttt{Passw0rd 1}. \newline 3. Bấm ``Đăng Ký''. \\
        Expected     & Từ chối vì email không hợp lệ. \\
        Actual       & Đăng ký thành công (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_a_03.png}\caption{Screenshot — BUG-A-03}\end{figure}

\subsection{BUG-A-04: Cho phép email trùng lặp}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-A-04 \\
        Tính năng    & FR-01 \quad\textbf{Kỹ thuật:} Domain \\
        Mức độ       & Critical \\
        Mô tả        & Cột \texttt{email} không \texttt{UNIQUE}; đăng ký trùng email tạo bản ghi thứ hai, phá vỡ định danh khi đăng nhập. \\
        Tái hiện     & 1. Đăng ký với \texttt{test@eshop.com} (đã tồn tại trong seed). \newline 2. Quan sát phản hồi. \\
        Expected     & Từ chối vì email đã tồn tại. \\
        Actual       & Tạo thêm một user thứ hai (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_a_04.png}\caption{Screenshot — BUG-A-04}\end{figure}

\subsection{BUG-A-05: Backend không kiểm tra dữ liệu (đăng ký body rỗng thành công)}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-A-05 \\
        Tính năng    & FR-01 \quad\textbf{Kỹ thuật:} Domain + BVA \\
        Mức độ       & Major \\
        Mô tả        & \texttt{POST /api/register} chèn thẳng vào DB, không kiểm rỗng/định dạng/độ dài; gọi API trực tiếp bỏ qua mọi kiểm tra của UI. \\
        Tái hiện     & 1. Gửi \texttt{POST /api/register} với body \texttt{\{\}} (hoặc mật khẩu \texttt{"1"}, hoặc chuỗi dài 100.000 ký tự). \\
        Expected     & \texttt{400 Bad Request}, từ chối. \\
        Actual       & \texttt{200 OK} ``User registered successfully'', tạo bản ghi (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_a_05.png}\caption{Screenshot — BUG-A-05}\end{figure}

\subsection{BUG-A-06: Mật khẩu lưu plaintext và trả về qua API}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-A-06 \\
        Tính năng    & FR-01 \quad\textbf{Kỹ thuật:} Domain \\
        Mức độ       & Critical \\
        Mô tả        & Mật khẩu được lưu dạng văn bản thuần và trả về nguyên văn trong \texttt{GET /api/users/me} và danh sách admin. \\
        Tái hiện     & 1. Đăng ký một tài khoản. \newline 2. Đăng nhập, gọi \texttt{GET /api/users/me}. \newline 3. Xem trường \texttt{password}. \\
        Expected     & Mật khẩu được băm; API không trả về mật khẩu. \\
        Actual       & Trả về mật khẩu plaintext (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_a_06.png}\caption{Screenshot — BUG-A-06}\end{figure}

% ============================================================
%  CHƯƠNG 3: FEATURE B — POOL B
% ============================================================
\chapter{Feature B — Mã giảm giá (Pool B)}

\section{Mô Tả Tính Năng}
Tính năng \textbf{FR-09 — Mã giảm giá} cho phép người dùng nhập một mã (\texttt{code})
để giảm giá cho đơn hàng có tổng tiền \texttt{total\_amount}. Logic cốt lõi nằm ở API
\texttt{POST /api/apply-coupon} (\texttt{backend/server.js}); dữ liệu mã lưu ở bảng
\texttt{coupons} (\texttt{backend/database.js}).

\begin{itemize}
    \item \textbf{Bảng \texttt{coupons}:} \texttt{code} (UNIQUE), \texttt{type}
    (\texttt{'percent'}/\texttt{'fixed'}), \texttt{discount\_value} (INTEGER),
    \texttt{min\_order\_amount} (INTEGER), \texttt{expired\_at} (DATETIME),
    \texttt{is\_active} (0/1), \texttt{max\_uses\_per\_user} (INTEGER).
    \item \textbf{API \texttt{apply-coupon}:} nhận \verb|{code, total_amount, user_id}|.
    Tìm mã đang \texttt{is\_active=1}; nếu \texttt{total\_amount > min\_order\_amount} thì
    kiểm tra hạn dùng, kiểm tra số lần dùng (chỉ khi có \texttt{user\_id}), rồi tính
    \texttt{discount\_amount} và \texttt{final\_amount}.
    \item \textbf{Các mã mẫu (seed):} \texttt{SAVE10} (percent 10, min 300k),
    \texttt{BIGBUY} (fixed 50k, min 500k), \texttt{VIP100} (fixed 100k, min 300k, tối đa 2 lần),
    \texttt{EXPIRED} (percent 20, hết hạn 2020).
\end{itemize}

\noindent\textbf{Đầu vào:} \texttt{code} (string), \texttt{total\_amount} (số, ₫),
\texttt{user\_id} (số hoặc vắng).\\
\textbf{Đầu ra:} mã HTTP, \texttt{discount\_amount}, \texttt{final\_amount}, thông báo.

\vspace{4pt}
\noindent\textbf{Công thức tính giảm giá theo mã nguồn:}
\begin{lstlisting}[caption={Tính discount trong apply-coupon (server.js)},numbers=none]
if (total_amount > coupon.min_order_amount) { ... }        // (A) dùng '>'
if (coupon.type === "percent")
    discount_amount = Math.floor(total_amount * (1 - coupon.discount_value)); // (B)
else
    discount_amount = coupon.discount_value;
final_amount = total_amount - discount_amount;
\end{lstlisting}

\begin{notebox}
Hai khiếm khuyết trọng tâm của chương này: \textbf{(A)} dùng toán tử \texttt{>} nên đơn hàng
\emph{bằng đúng} \texttt{min\_order\_amount} bị từ chối (lỗi off-by-one biên). \textbf{(B)}
\texttt{discount\_value} của mã \texttt{percent} lưu dạng số nguyên (\texttt{10} nghĩa là 10\%),
nhưng công thức lại là \texttt{total\_amount $\times$ (1 $-$ discount\_value)}. Với
\texttt{SAVE10} (10\%) trên đơn 400.000\,₫: đáng lẽ giảm 40.000 (còn 360.000), nhưng mã tính
\texttt{floor(400000 $\times$ (1$-$10)) $= -3.600.000$}, khiến \texttt{final\_amount $=
400000 - (-3.600.000) = 4.000.000$} — \emph{đắt gấp 10 lần}. Công thức chỉ đúng cho mã
\texttt{fixed}.
\end{notebox}

\begin{notebox}
\textbf{Quy ước cột kết quả} (như Feature A): cột \emph{Kết quả}/\emph{Trạng thái} là kết quả
\textbf{thực thi thật} (2026-07-02) trên API \texttt{POST /api/apply-coupon}
(\texttt{localhost:3000}); mọi kết quả khớp dự đoán từ mã nguồn. Cột
\emph{Expected} là hành vi \emph{đúng theo đặc tả}.
\end{notebox}

\section{Domain Testing}

Áp dụng Domain Testing theo bốn bước của bài giảng (\emph{xác định biến $\to$ phân hoạch miền
$\to$ chọn điểm ON/OFF/IN/OUT $\to$ thiết kế ca}). Với FR-09, biến có thứ tự trọng tâm là
\texttt{total\_amount} (biên tại \texttt{min\_order\_amount}) và \texttt{max\_uses\_per\_user};
các biến rời rạc (\texttt{code}, \texttt{type}, \texttt{is\_active}, \texttt{expiry}) được phủ
bằng phân hoạch tương đương --- mỗi trạng thái một lớp. Biến \emph{đầu ra} (\texttt{discount\_amount},
\texttt{final\_amount}) cũng được đưa vào để bắt lỗi công thức. Bước~5 tổng hợp ma trận truy vết
EC $\to$ TC.

\subsection{Bước 1: Xác Định Các Biến Đầu Vào}
\begin{table}[H]
    \centering
    \caption{Danh sách biến — Feature B (FR-09)}
    \begin{tabularx}{\linewidth}{C{0.5cm} L{3cm} L{2.6cm} C{1.8cm} X}
        \toprule
        \textbf{\#} & \textbf{Biến} & \textbf{Kiểu} & \textbf{Vào/Ra} & \textbf{Ghi chú} \\
        \midrule
        1 & code & string & Input & Bắt buộc; so khớp phân biệt hoa/thường (SQLite BINARY). \\
        2 & total\_amount & integer (₫) & Input & Biến có thứ tự $\Rightarrow$ biên tại \texttt{min\_order\_amount}. \\
        3 & user\_id & integer / vắng & Input & Quyết định có kiểm tra số lần dùng hay không. \\
        4 & coupon.type & enum & Dữ liệu mã & \texttt{percent} (công thức sai) vs \texttt{fixed} (đúng). \\
        5 & coupon.discount\_value & integer & Dữ liệu mã & percent: 10 = 10\%; fixed: số tiền ₫. \\
        6 & coupon.min\_order\_amount & integer & Dữ liệu mã & Biên (so bằng \texttt{>}). \\
        7 & coupon.expired\_at & date & Dữ liệu mã & Biên so với \emph{now}. \\
        8 & coupon.max\_uses\_per\_user & integer & Dữ liệu mã & Biên số lần dùng (\texttt{>=}). \\
        9 & coupon.is\_active & boolean & Dữ liệu mã & \texttt{0} $\Rightarrow$ coi như không tồn tại. \\
        10 & discount\_amount / final\_amount & integer & \textbf{Output} & Biên đầu ra: không âm, final $\ge 0$. \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Bước 2: Phân Hoạch Miền}
Dấu \emph{*} đánh dấu lớp mà một hệ thống \emph{đúng} phải xử lý theo đặc tả nhưng mã hiện tại
xử lý sai.

\subsubsection{Biến code}
\begin{table}[H]
    \centering
    \caption{Phân hoạch miền — code}
    \begin{tabularx}{\linewidth}{C{1.4cm} L{6cm} C{1.6cm} X}
        \toprule
        \textbf{EC} & \textbf{Điều kiện} & \textbf{Loại} & \textbf{Ví dụ} \\
        \midrule
        EC-C1 & Mã tồn tại, đang \texttt{is\_active=1} & Valid & \texttt{SAVE10} \\
        EC-C2 & Mã không tồn tại & Invalid & \texttt{NOPE99} \\
        EC-C3 & Rỗng / thiếu trường & Invalid & \verb|""| \\
        EC-C4 & Mã tồn tại nhưng \texttt{is\_active=0} & Invalid & (mã bị vô hiệu) \\
        EC-C5 & Đúng mã nhưng khác hoa/thường & Invalid* & \texttt{save10} \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsubsection{Biến total\_amount (so với \texttt{min\_order\_amount})}
\begin{table}[H]
    \centering
    \caption{Phân hoạch miền — total\_amount (mã VIP100, min = 300.000)}
    \begin{tabularx}{\linewidth}{C{1.4cm} L{6cm} C{1.6cm} X}
        \toprule
        \textbf{EC} & \textbf{Điều kiện} & \textbf{Loại} & \textbf{Ví dụ} \\
        \midrule
        EC-T1 & \texttt{total > min} & Valid & 300.001 \\
        EC-T2 & \texttt{total = min} (đúng biên) & Valid* & 300.000 \\
        EC-T3 & \texttt{total < min} & Invalid & 299.999 \\
        EC-T4 & Âm hoặc 0 & Invalid & \texttt{-100} \\
        EC-T5 & Thiếu / không phải số & Invalid & \texttt{undefined} \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsubsection{Biến user\_id \& loại mã}
\begin{table}[H]
    \centering
    \caption{Phân hoạch miền — user\_id, type, expiry}
    \begin{tabularx}{\linewidth}{C{1.4cm} L{6.5cm} C{1.6cm} X}
        \toprule
        \textbf{EC} & \textbf{Điều kiện} & \textbf{Loại} & \textbf{Ví dụ} \\
        \midrule
        EC-U1 & Có \texttt{user\_id}, chưa đạt \texttt{max\_uses} & Valid & dùng lần 1/2 \\
        EC-U2 & Có \texttt{user\_id}, đã đạt \texttt{max\_uses} & Invalid & dùng lần 3 (VIP100) \\
        EC-U3 & \emph{Vắng} \texttt{user\_id} $\Rightarrow$ bỏ qua kiểm tra & Invalid* & không gửi \texttt{user\_id} \\
        EC-Y1 & Loại \texttt{fixed} & Valid & VIP100/BIGBUY \\
        EC-Y2 & Loại \texttt{percent} (công thức sai) & Valid* & SAVE10 \\
        EC-X1 & Còn hạn & Valid & \texttt{2099-12-31} \\
        EC-X2 & Hết hạn & Invalid & EXPIRED (2020) \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Bước 3: Chọn Điểm Kiểm Thử (ON/OFF/IN/OUT)}
Biên số học rõ ràng nhất là \texttt{total\_amount} tại \texttt{min\_order\_amount}. Dùng mã
\textbf{VIP100} (\texttt{fixed} 100.000, min 300.000) để cô lập biên chấp nhận/từ chối, tránh
để lỗi công thức percent làm nhiễu.

\begin{table}[H]
    \centering
    \caption{Điểm ON/OFF/IN/OUT — total\_amount (min = 300.000)}
    \begin{tabularx}{\linewidth}{L{2.4cm} C{2.4cm} X}
        \toprule
        \textbf{Loại điểm} & \textbf{Giá trị} & \textbf{Kỳ vọng (đặc tả)} \\
        \midrule
        ON-point  & 300.000 & Chấp nhận (đơn đủ mức tối thiểu) \\
        OFF-point & 299.999 & Từ chối (dưới mức tối thiểu) \\
        IN-point  & 400.000 & Chấp nhận \\
        OUT-point & 0       & Từ chối \\
        \bottomrule
    \end{tabularx}
\end{table}

\noindent\textbf{Nhận xét:} do mã dùng \texttt{total\_amount > min} (toán tử \texttt{>}),
\emph{ON-point} 300.000 bị \emph{từ chối} trong khi đặc tả yêu cầu \emph{chấp nhận} — đây là
lỗi off-by-one biên (BUG-B-02). Biên \texttt{max\_uses\_per\_user} lại dùng \texttt{>=} nên
được cài đặt \emph{đúng} (sẽ Pass), ngoại trừ lỗ hổng bỏ qua khi vắng \texttt{user\_id}.

\subsection{Bước 4: Thiết Kế Test Cases — Domain Testing}
\begin{longtable}{C{1.1cm} L{3.1cm} L{3.2cm} L{3cm} C{2cm} C{1.4cm}}
    \caption{Test cases — Domain Testing, Feature B} \\
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endfirsthead
    \multicolumn{6}{c}{\small\itshape (tiếp theo)} \\
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endhead
    \midrule
    \multicolumn{6}{r}{\small\itshape (còn tiếp)} \\
    \endfoot
    \bottomrule
    \endlastfoot
    TC-B-DT-01 & Mã percent hợp lệ \newline (BUG-B-01) & SAVE10, total=400.000 & discount 40.000, final 360.000 & discount $-3.600.000$, final 4.000.000 & \textcolor{red}{Fail} \\
    TC-B-DT-02 & Mã fixed hợp lệ & BIGBUY, total=600.000 & discount 50.000, final 550.000 & Như expected & \textcolor{green!60!black}{Pass} \\
    TC-B-DT-03 & Mã fixed hợp lệ & VIP100, total=400.000 & discount 100.000, final 300.000 & Như expected & \textcolor{green!60!black}{Pass} \\
    TC-B-DT-04 & Mã không tồn tại & NOPE99, total=400.000 & 404 không tồn tại & 404 & \textcolor{green!60!black}{Pass} \\
    TC-B-DT-05 & Thiếu mã & code=\verb|""| & 400 ``Vui lòng nhập mã'' & 400 & \textcolor{green!60!black}{Pass} \\
    TC-B-DT-06 & Mã hết hạn & EXPIRED, total=200.000 & 400 ``đã hết hạn'' & 400 & \textcolor{green!60!black}{Pass} \\
    TC-B-DT-07 & total = min (đúng biên) \newline (BUG-B-02) & VIP100, total=300.000 & Chấp nhận (final 200.000) & 400 ``chưa đủ giá trị'' & \textcolor{red}{Fail} \\
    TC-B-DT-08 & Đạt giới hạn số lần dùng & VIP100, user\_id=U, đã dùng 2 lần & 400 ``đã đạt giới hạn'' & 400 & \textcolor{green!60!black}{Pass} \\
    TC-B-DT-09 & Bỏ qua giới hạn khi vắng user\_id \newline (BUG-B-03) & VIP100, \emph{không} gửi user\_id, đã dùng 2 lần & 400 ``đã đạt giới hạn'' & Áp dụng OK (bỏ kiểm tra) & \textcolor{red}{Fail} \\
    TC-B-DT-10 & Không xác thực, giả mạo user\_id \newline (BUG-B-04) & apply-coupon với user\_id tùy ý (không token) & Lấy user từ token & Tin user\_id trong body & \textcolor{red}{Fail} \\
    TC-B-DT-11 & Mã khác hoa/thường & \texttt{save10}, total=400.000 & Tùy đặc tả (nên không phân biệt) & 404 không tồn tại & \textcolor{orange}{Note} \\
    TC-B-DT-12 & fixed vượt tổng đơn \newline (BUG-B-05) & mã fixed (discount 100.000, min 50.000), total=60.000 & final $\ge 0$, có chặn & final $= -40.000$ (không chặn) & \textcolor{red}{Fail} \\
\end{longtable}

\subsection{Bước 5: Ma Trận Truy Vết Độ Phủ (EC $\to$ Test Case)}
Bảng dưới đối chiếu mọi lớp tương đương (Bước~2) với ca kiểm thử phủ nó (kể cả ca BVA). Các lớp
cùng nhánh xử lý dùng chung ca đại diện; một lớp chưa có ca riêng được ghi nhận minh bạch để bổ
sung.

\begin{table}[H]
    \centering
    \caption{Truy vết EC $\to$ TC — Feature B (FR-09)}
    \begin{tabularx}{\linewidth}{C{1.8cm} C{3.4cm} X}
        \toprule
        \textbf{EC} & \textbf{Ca phủ (DT/BVA)} & \textbf{Ghi chú} \\
        \midrule
        EC-C1 & TC-B-DT-01/02/03 & Mã tồn tại, đang \texttt{is\_active=1}. \\
        EC-C2 & TC-B-DT-04 & Mã không tồn tại. \\
        EC-C3 & TC-B-DT-05 & Thiếu mã / rỗng. \\
        EC-C4 & TC-B-DT-04 (đại diện) & Truy vấn lọc \texttt{is\_active=1} nên mã inactive đi cùng nhánh ``không tồn tại''; không có mã seed inactive. \\
        EC-C5 & TC-B-DT-11 & Khác hoa/thường (BINARY). \\
        EC-T1 & TC-B-DT-02/03, BV-03 & \texttt{total > min}. \\
        EC-T2 & TC-B-DT-07, BV-02 & \texttt{total = min} (biên off-by-one). \\
        EC-T3 & TC-B-BV-01 & \texttt{total < min}. \\
        EC-T4 & TC-B-BV-01 (đại diện) & 0/âm $<$ min $\Rightarrow$ cùng nhánh từ chối như EC-T3 (OUT-point ở Bước~3). \\
        EC-T5 & --- & Chưa có ca riêng (phụ thuộc parse của Express khi \texttt{total\_amount} thiếu/không phải số); ghi nhận là điểm cần bổ sung. \\
        EC-U1 & TC-B-DT-03, BV-04 & Có \texttt{user\_id}, chưa đạt max. \\
        EC-U2 & TC-B-DT-08, BV-05 & Có \texttt{user\_id}, đã đạt max. \\
        EC-U3 & TC-B-DT-09/10 & Vắng \texttt{user\_id} $\Rightarrow$ bỏ kiểm tra. \\
        EC-Y1 & TC-B-DT-02/03 & Loại \texttt{fixed}. \\
        EC-Y2 & TC-B-DT-01 & Loại \texttt{percent} (công thức sai). \\
        EC-X1 & TC-B-BV-06 & Còn hạn. \\
        EC-X2 & TC-B-DT-06, BV-06 & Hết hạn. \\
        \bottomrule
    \end{tabularx}
\end{table}
\noindent\textbf{Kết luận độ phủ:} 16/17 lớp có ca phủ trực tiếp hoặc đại diện; riêng EC-T5
(\texttt{total\_amount} không phải số) được ghi nhận là ca cần bổ sung.

\section{Boundary Value Analysis (BVA)}

BVA cho FR-09 nhắm vào ba biên: \texttt{total\_amount} so với \texttt{min\_order\_amount},
\texttt{max\_uses\_per\_user}, và \texttt{expired\_at} so với \emph{now}. Với mỗi biên số, ta sinh
bộ ba \texttt{off/on/on$+1$} để lộ off-by-one --- điển hình là toán tử \texttt{>} tại biên
\texttt{min} khiến đơn bằng đúng mức tối thiểu bị từ chối. Bước~3 tổng hợp ma trận phủ biên, cho
thấy mỗi biên đều có đủ điểm dưới/trên.

\subsection{Bước 1: Xác Định Ranh Giới}
\begin{table}[H]
    \centering
    \caption{Bảng giá trị biên — Feature B (FR-09)}
    \begin{tabularx}{\linewidth}{L{3.4cm} C{1.8cm} C{1.8cm} C{1.8cm} X}
        \toprule
        \textbf{Biên} & \textbf{Off} & \textbf{On} & \textbf{On+1} & \textbf{Ghi chú} \\
        \midrule
        total\_amount vs min (VIP100=300k) & 299.999 & 300.000 & 300.001 & On bị từ chối (dùng \texttt{>}) $\Rightarrow$ lỗi. \\
        max\_uses\_per\_user (VIP100=2) & 1 lần & 2 lần & 3 lần & Dùng \texttt{>=}, cài đặt đúng. \\
        expired\_at vs now & quá khứ & --- & tương lai & So sánh \texttt{expiry < now} đúng. \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Bước 2: Thiết Kế Test Cases — BVA}
\begin{longtable}{C{1.1cm} L{3.2cm} L{3.2cm} L{3cm} C{2cm} C{1.4cm}}
    \caption{Test cases — BVA, Feature B} \\
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endfirsthead
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endhead
    \bottomrule
    \endlastfoot
    TC-B-BV-01 & min$-1$: dưới biên & VIP100, total=299.999 & Từ chối (chưa đủ) & Từ chối & \textcolor{green!60!black}{Pass} \\
    TC-B-BV-02 & min: đúng biên \newline (BUG-B-02) & VIP100, total=300.000 & Chấp nhận, final 200.000 & Từ chối ``chưa đủ'' & \textcolor{red}{Fail} \\
    TC-B-BV-03 & min$+1$: trên biên & VIP100, total=300.001 & Chấp nhận, final 200.001 & Chấp nhận & \textcolor{green!60!black}{Pass} \\
    TC-B-BV-04 & max$-1$ lần dùng & VIP100, user\_id=U, đã dùng 1 & Chấp nhận & Chấp nhận & \textcolor{green!60!black}{Pass} \\
    TC-B-BV-05 & max lần dùng (đúng biên) & VIP100, user\_id=U, đã dùng 2 & Từ chối ``đạt giới hạn'' & Từ chối & \textcolor{green!60!black}{Pass} \\
    TC-B-BV-06 & Biên hạn dùng & EXPIRED (quá khứ) vs SAVE10 (tương lai) & Quá khứ từ chối, tương lai nhận & Đúng như vậy & \textcolor{green!60!black}{Pass} \\
\end{longtable}

\subsection{Bước 3: Ma Trận Phủ Biên (off / on / on$+1$)}
Bảng dưới chứng minh mỗi biên ở Bước~1 đều được kiểm đủ ba giá trị, lấy ca từ cả bảng BVA lẫn
Domain Testing.

\begin{table}[H]
    \centering
    \caption{Phủ biên — Feature B (FR-09)}
    \begin{tabularx}{\linewidth}{L{2.8cm} C{1.9cm} C{1.7cm} C{1.6cm} X}
        \toprule
        \textbf{Biên} & \textbf{Off} & \textbf{On} & \textbf{On$+1$} & \textbf{Ngoài / Ghi chú} \\
        \midrule
        total\_amount vs min (300k) & BV-01 (299.999) & BV-02 (300.000) & BV-03 (300.001) & Ngoài miền: 0/âm (OUT-point Bước~3). Off-by-one tại On (dùng \texttt{>}). \\
        max\_uses (2) & BV-04 (1) & BV-05 (2) & DT-08 (3) & On$+1$ = lần dùng thứ 3 bị từ chối (đúng, dùng \texttt{>=}). \\
        expired\_at vs now & DT-06/BV-06 (quá khứ) & \emph{now} & BV-06 (tương lai) & Biên thời gian: quá khứ từ chối, tương lai nhận. \\
        \bottomrule
    \end{tabularx}
\end{table}
\noindent\textbf{Kết luận:} cả ba biên đều có điểm dưới/trên; biên \texttt{min\_order\_amount}
lộ off-by-one (BUG-B-02), còn \texttt{max\_uses} và \texttt{expiry} được cài đặt đúng.

\section{AI Gap Analysis}
\begin{table}[H]
    \centering
    \caption{AI Gap Analysis — Feature B (FR-09)}
    \begin{tabularx}{\linewidth}{C{0.5cm} X L{4cm} C{1.8cm}}
        \toprule
        \textbf{\#} & \textbf{Ca/bug bị bỏ sót} & \textbf{Lý do bỏ sót} & \textbf{Bổ sung ở TC} \\
        \midrule
        1 & Công thức percent sai (\texttt{1$-$discount\_value}) làm giá tăng gấp $\sim$10 lần — cần đọc \emph{phép tính thực tế}, không chỉ tên biến. & Giới hạn công cụ + độ phức tạp (biến \emph{đầu ra}, không phải input). & TC-B-DT-01 \\
        2 & Off-by-one biên \texttt{min\_order\_amount} (\texttt{>} thay vì \texttt{>=}) — chỉ lộ khi thử \emph{đúng} giá trị biên. & Chất lượng prompt (không yêu cầu thử ca ``bằng đúng biên''). & TC-B-DT-07, BV-02 \\
        3 & Bỏ qua giới hạn số lần dùng khi \emph{vắng} \texttt{user\_id} (nhánh else). & Độ phức tạp nội tại (phụ thuộc luồng điều khiển/nhánh). & TC-B-DT-09 \\
        4 & \texttt{apply-coupon} không có \texttt{authenticateToken}; tin \texttt{user\_id} từ body $\Rightarrow$ giả mạo/bỏ giới hạn. & Giới hạn công cụ (AI tập trung input, bỏ sót thiếu middleware). & TC-B-DT-10 \\
        5 & Không chặn \texttt{final\_amount} âm / \texttt{discount > total} với mã fixed. & Độ phức tạp (biên đầu ra + tổ hợp hiếm). & TC-B-DT-12 \\
        6 & So khớp \texttt{code} phân biệt hoa/thường (SQLite BINARY) — \texttt{save10} $\ne$ \texttt{SAVE10}. & Độ phức tạp (locale/encoding, hành vi ngầm của SQLite). & TC-B-DT-11 \\
        \bottomrule
    \end{tabularx}
\end{table}

\section{Bug Reports — Feature B}
Năm lỗi ứng viên dưới đây suy ra từ phân tích mã nguồn; cần \textbf{xác nhận bằng thực thi},
chụp màn hình và tạo GitHub Issue trước khi nộp.

\subsection{BUG-B-01: Mã giảm giá phần trăm tính sai, làm tăng giá $\sim$10 lần}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-B-01 \\
        Tính năng    & FR-09 \quad\textbf{Kỹ thuật:} Domain \\
        Mức độ       & Critical \\
        Mô tả        & Công thức \texttt{floor(total\_amount $\times$ (1 $-$ discount\_value))} sai: \texttt{discount\_value} là số nguyên (10 = 10\%), khiến \texttt{discount\_amount} âm và \texttt{final\_amount} lớn gấp $\sim$10 lần đơn gốc. \\
        Tái hiện     & 1. Thêm hàng để tổng $=400.000$. \newline 2. Nhập mã \texttt{SAVE10}. \newline 3. Bấm áp dụng. \\
        Expected     & Giảm 40.000, còn 360.000. \\
        Actual       & \texttt{discount\_amount} $=-3.600.000$, \texttt{final\_amount} $=4.000.000$ (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_b_01.png}\caption{Screenshot — BUG-B-01}\end{figure}

\subsection{BUG-B-02: Đơn bằng đúng mức tối thiểu bị từ chối (off-by-one)}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-B-02 \\
        Tính năng    & FR-09 \quad\textbf{Kỹ thuật:} BVA \\
        Mức độ       & Minor \\
        Mô tả        & Điều kiện \texttt{total\_amount > min\_order\_amount} dùng \texttt{>} thay vì \texttt{>=}; đơn bằng đúng mức tối thiểu bị từ chối. \\
        Tái hiện     & 1. Đặt tổng đơn $=300.000$. \newline 2. Nhập mã \texttt{VIP100} (min 300.000). \newline 3. Áp dụng. \\
        Expected     & Chấp nhận (đơn đủ mức tối thiểu), final 200.000. \\
        Actual       & 400 ``Đơn hàng chưa đủ giá trị tối thiểu\ldots'' (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_b_02.png}\caption{Screenshot — BUG-B-02}\end{figure}

\subsection{BUG-B-03: Bỏ qua giới hạn số lần dùng khi thiếu user\_id}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-B-03 \\
        Tính năng    & FR-09 \quad\textbf{Kỹ thuật:} Domain \\
        Mức độ       & Major \\
        Mô tả        & Kiểm tra \texttt{max\_uses\_per\_user} chỉ chạy trong nhánh \texttt{if (user\_id)}; nếu không gửi \texttt{user\_id}, kiểm tra bị bỏ qua và mã dùng được không giới hạn. \\
        Tái hiện     & 1. Dùng \texttt{VIP100} đủ 2 lần (đạt giới hạn). \newline 2. Gọi \texttt{apply-coupon} \emph{không} kèm \texttt{user\_id}. \\
        Expected     & 400 ``đã đạt giới hạn''. \\
        Actual       & Áp dụng thành công (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_b_03.png}\caption{Screenshot — BUG-B-03}\end{figure}

\subsection{BUG-B-04: apply-coupon không xác thực, tin user\_id từ client}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-B-04 \\
        Tính năng    & FR-09 \quad\textbf{Kỹ thuật:} Domain \\
        Mức độ       & Major \\
        Mô tả        & \texttt{POST /api/apply-coupon} không có middleware \texttt{authenticateToken}; \texttt{user\_id} lấy trực tiếp từ body nên có thể giả mạo hoặc bỏ trống để né giới hạn dùng. \\
        Tái hiện     & 1. Gọi \texttt{apply-coupon} với \texttt{user\_id} bất kỳ (không token). \newline 2. Quan sát mã vẫn áp dụng. \\
        Expected     & Xác thực token, lấy \texttt{user\_id} từ token. \\
        Actual       & Chấp nhận \texttt{user\_id} tùy client (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_b_04.png}\caption{Screenshot — BUG-B-04}\end{figure}

\subsection{BUG-B-05: Không chặn final\_amount âm với mã fixed}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-B-05 \\
        Tính năng    & FR-09 \quad\textbf{Kỹ thuật:} BVA \\
        Mức độ       & Minor \\
        Mô tả        & Với mã \texttt{fixed}, \texttt{discount\_amount = discount\_value} không bị chặn theo \texttt{total\_amount}; nếu số tiền giảm gần bằng tổng đơn, \texttt{final\_amount} có thể còn $\approx 0$ hoặc âm, không có kẹp $\ge 0$. \\
        Tái hiện     & 1. Tạo mã fixed có \texttt{discount\_value} $>$ \texttt{min\_order\_amount} (qua trang admin coupon), vd \texttt{discount=100.000, min=50.000}. \newline 2. Áp mã cho đơn \texttt{total=60.000} ($>$ min nên qua kiểm tra). \\
        Expected     & \texttt{final\_amount} được kẹp $\ge 0$ và hợp lý. \\
        Actual       & \texttt{final\_amount} $= -40.000$ (âm, không kẹp; xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_b_05.png}\caption{Screenshot — BUG-B-05}\end{figure}

% ============================================================
%  CHƯƠNG 4: FEATURE C — POOL C
% ============================================================
\chapter{Feature C — Quản lý danh mục (Pool C — Web Admin)}

\section{Mô Tả Tính Năng}
Tính năng \textbf{FR-14 — Quản lý danh mục (CRUD)} thuộc trang \emph{Web Admin}, cho phép
thêm/sửa/xóa danh mục sản phẩm. Toàn bộ logic nằm ở 4 endpoint trong
\texttt{backend/server.js}; bảng \texttt{categories} khai báo ở \texttt{backend/database.js}.

\begin{itemize}
    \item \texttt{GET /api/categories} — \emph{công khai} (không xác thực), trả về mọi danh mục.
    \item \texttt{POST /api/categories} — \texttt{authenticateToken}, body \verb|{name}| $\to$
    \texttt{INSERT INTO categories (name)}.
    \item \texttt{PUT /api/categories/:id} — \texttt{authenticateToken}, \verb|{name}| $\to$
    \texttt{UPDATE categories SET name=? WHERE id=?}.
    \item \texttt{DELETE /api/categories/:id} — \texttt{authenticateToken} $\to$
    \texttt{DELETE FROM categories WHERE id=?}.
\end{itemize}

\noindent\textbf{Lược đồ:} \texttt{categories(id INTEGER PK AUTOINCREMENT, name TEXT)} — cột
\texttt{name} \emph{không} \texttt{NOT NULL}, \emph{không} \texttt{UNIQUE}, không giới hạn độ
dài. Bảng \texttt{products} có \texttt{category\_id INTEGER} nhưng \emph{không có khóa ngoại}
(foreign key) trỏ tới \texttt{categories}. \textbf{Seed:} 3 danh mục — \texttt{1=Điện thoại}
(có 2 sản phẩm), \texttt{2=Laptop} (1 sản phẩm), \texttt{3=Phụ kiện} (2 sản phẩm).

\vspace{4pt}
\noindent\textbf{Middleware \texttt{authenticateToken}} chỉ kiểm tra JWT \emph{hợp lệ}
(\texttt{jwt.verify}) rồi gán \texttt{req.user}; \textbf{không} kiểm tra \texttt{role ===
"admin"}. Do đó \emph{bất kỳ} người dùng đã đăng nhập (kể cả \texttt{role='user'}) đều gọi được
các endpoint quản trị này.

\begin{notebox}
Khiếm khuyết trọng tâm của chương: \textbf{(1)} thiếu kiểm soát truy cập theo vai trò — CRUD
danh mục (chức năng admin) mở cho mọi tài khoản đăng nhập; \textbf{(2)} không kiểm tra dữ liệu
\texttt{name} (rỗng/trùng/quá dài); \textbf{(3)} PUT/DELETE trên \texttt{id} không tồn tại vẫn
trả \texttt{200} thành công (không kiểm \texttt{this.changes}); \textbf{(4)} xóa danh mục làm
\emph{mồ côi} sản phẩm (không khóa ngoại/cascade).
\end{notebox}

\begin{notebox}
\textbf{Quy ước cột kết quả} (như Feature A/B): cột \emph{Kết quả}/\emph{Trạng thái} là kết quả
\textbf{thực thi thật} (2026-07-02) trên 4 endpoint \texttt{/api/categories}
(\texttt{localhost:3000}, dùng cả token admin lẫn token user); mọi kết quả khớp dự đoán.
\end{notebox}

\section{Domain Testing}

Áp dụng Domain Testing theo bốn bước của bài giảng (\emph{xác định biến $\to$ phân hoạch miền
$\to$ chọn điểm ON/OFF/IN/OUT $\to$ thiết kế ca}). Với FR-14, biến có thứ tự là \texttt{id}
(ranh giới \emph{tồn tại} theo seed $[1,3]$) và \texttt{name.length}; biến phân loại quan trọng
nhất là \texttt{token.role} (phân quyền admin/user). Vì đây là CRUD, ta phủ cả bốn thao tác
(POST/PUT/DELETE + trạng thái ``có sản phẩm'') và các lớp quyền truy cập. Bước~5 tổng hợp ma
trận truy vết EC $\to$ TC.

\subsection{Bước 1: Xác Định Các Biến Đầu Vào}
\begin{table}[H]
    \centering
    \caption{Danh sách biến — Feature C (FR-14)}
    \begin{tabularx}{\linewidth}{C{0.5cm} L{3cm} L{2.6cm} C{1.8cm} X}
        \toprule
        \textbf{\#} & \textbf{Biến} & \textbf{Kiểu} & \textbf{Vào/Ra} & \textbf{Ghi chú} \\
        \midrule
        1 & name & string (TEXT) & Input & POST/PUT; không kiểm rỗng/trùng/độ dài. \\
        2 & id & integer (path) & Input & PUT/DELETE; phải tồn tại (seed 1..3). \\
        3 & token.role & string (JWT) & Input & \emph{Nên} yêu cầu \texttt{admin}; code chỉ cần token hợp lệ. \\
        4 & name.length & integer (ẩn) & Input & Không có min ($>0$)/max $\Rightarrow$ dùng BVA. \\
        5 & name.isUnique & boolean (ẩn) & Input & Không có \texttt{UNIQUE} $\Rightarrow$ cho trùng. \\
        6 & category.hasProducts & boolean (ẩn) & Trạng thái & Ảnh hưởng toàn vẹn khi xóa. \\
        7 & HTTP status / changes & integer & \textbf{Output} & 200/401/403; số dòng bị tác động. \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Bước 2: Phân Hoạch Miền}
Dấu \emph{*} = lớp mà hệ thống \emph{đúng} phải xử lý theo đặc tả nhưng mã hiện tại làm sai.

\subsubsection{Biến name}
\begin{table}[H]
    \centering
    \caption{Phân hoạch miền — name}
    \begin{tabularx}{\linewidth}{C{1.4cm} L{6cm} C{1.6cm} X}
        \toprule
        \textbf{EC} & \textbf{Điều kiện} & \textbf{Loại} & \textbf{Ví dụ} \\
        \midrule
        EC-NM1 & Tên mới, không rỗng, chưa trùng & Valid & \texttt{Sách} \\
        EC-NM2 & Rỗng & Invalid & \verb|""| \\
        EC-NM3 & Chỉ khoảng trắng & Invalid & \verb|"   "| \\
        EC-NM4 & Thiếu trường (\texttt{undefined}$\to$NULL) & Invalid & \texttt{POST \{\}} \\
        EC-NM5 & Trùng tên đã tồn tại & Invalid* & \texttt{Laptop} \\
        EC-NM6 & Rất dài (không có max) & Valid* & 100.000 ký tự \\
        EC-NM7 & Payload injection & Invalid & \texttt{x'; DROP TABLE categories;-{}-} \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsubsection{Biến id (PUT/DELETE)}
\begin{table}[H]
    \centering
    \caption{Phân hoạch miền — id}
    \begin{tabularx}{\linewidth}{C{1.4cm} L{6cm} C{1.6cm} X}
        \toprule
        \textbf{EC} & \textbf{Điều kiện} & \textbf{Loại} & \textbf{Ví dụ} \\
        \midrule
        EC-ID1 & id tồn tại (seed) & Valid & \texttt{1} \\
        EC-ID2 & id không tồn tại & Invalid* & \texttt{999} \\
        EC-ID3 & id không phải số & Invalid & \texttt{abc} \\
        EC-ID4 & id danh mục \emph{đang có sản phẩm} & Invalid* & \texttt{1} (khi xóa) \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsubsection{Biến quyền (token/role)}
\begin{table}[H]
    \centering
    \caption{Phân hoạch miền — quyền truy cập}
    \begin{tabularx}{\linewidth}{C{1.4cm} L{6.5cm} C{1.6cm} X}
        \toprule
        \textbf{EC} & \textbf{Điều kiện} & \textbf{Loại} & \textbf{Kỳ vọng} \\
        \midrule
        EC-AU1 & Token admin & Valid & Cho phép \\
        EC-AU2 & Token user thường & Invalid* & \emph{Nên} 403; code cho qua \\
        EC-AU3 & Không có token & Invalid & 401 (đúng) \\
        EC-AU4 & Token sai/hết hạn & Invalid & 403 (đúng) \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Bước 3: Chọn Điểm Kiểm Thử (ON/OFF/IN/OUT)}
Biến có thứ tự là \texttt{id} với miền hợp lệ (danh mục tồn tại) $= [1,3]$ theo seed. Áp dụng
ON/OFF/IN/OUT trên \emph{ranh giới tồn tại}.

\begin{table}[H]
    \centering
    \caption{Điểm ON/OFF/IN/OUT — id danh mục (miền tồn tại $[1,3]$)}
    \begin{tabularx}{\linewidth}{L{2.4cm} C{2cm} X}
        \toprule
        \textbf{Loại điểm} & \textbf{id} & \textbf{Kỳ vọng (đặc tả)} \\
        \midrule
        ON (min)   & 1 & Tồn tại $\to$ sửa/xóa OK \\
        ON (max)   & 3 & Tồn tại $\to$ sửa/xóa OK \\
        OFF (dưới) & 0 & Không tồn tại $\to$ \texttt{404} \\
        OFF (trên) & 4 & Không tồn tại $\to$ \texttt{404} \\
        OUT        & 999 & Không tồn tại $\to$ \texttt{404} \\
        \bottomrule
    \end{tabularx}
\end{table}

\noindent\textbf{Nhận xét:} mã \emph{không} kiểm \texttt{this.changes} nên mọi điểm OFF/OUT
(id không tồn tại) đều trả \texttt{200 ``updated/deleted''} thay vì \texttt{404} — lỗi ``thành
công giả'' (BUG-C-04).

\subsection{Bước 4: Thiết Kế Test Cases — Domain Testing}
\begin{longtable}{C{1.2cm} L{3.1cm} L{3.1cm} L{3cm} C{1.9cm} C{1.4cm}}
    \caption{Test cases — Domain Testing, Feature C} \\
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endfirsthead
    \multicolumn{6}{c}{\small\itshape (tiếp theo)} \\
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endhead
    \midrule
    \multicolumn{6}{r}{\small\itshape (còn tiếp)} \\
    \endfoot
    \bottomrule
    \endlastfoot
    TC-C-DT-01 & Tạo danh mục hợp lệ (admin) & POST \texttt{\{name:"Sách"\}} & 200, trả \texttt{id} & 200 & \textcolor{green!60!black}{Pass} \\
    TC-C-DT-02 & Tạo với tên rỗng \newline (BUG-C-02) & POST \texttt{\{name:""\}} & 400 từ chối & 200 tạo bản ghi rỗng & \textcolor{red}{Fail} \\
    TC-C-DT-03 & Tạo thiếu name \newline (BUG-C-02) & POST \texttt{\{\}} & 400 từ chối & 200, \texttt{name=NULL} & \textcolor{red}{Fail} \\
    TC-C-DT-04 & Tạo tên trùng \newline (BUG-C-03) & POST \texttt{\{name:"Laptop"\}} & Từ chối trùng & 200 tạo ``Laptop'' thứ 2 & \textcolor{red}{Fail} \\
    TC-C-DT-05 & Tạo bằng token user thường \newline (BUG-C-01) & POST (role=user) & 403 Forbidden & 200 tạo thành công & \textcolor{red}{Fail} \\
    TC-C-DT-06 & Tạo khi không có token & POST (no auth) & 401 Unauthorized & 401 & \textcolor{green!60!black}{Pass} \\
    TC-C-DT-07 & Tạo với token sai & POST (token hỏng) & 403 Forbidden & 403 & \textcolor{green!60!black}{Pass} \\
    TC-C-DT-08 & Sửa danh mục tồn tại & PUT \texttt{/1 \{name:"Điện thoại"\}} & 200 updated & 200 & \textcolor{green!60!black}{Pass} \\
    TC-C-DT-09 & Sửa id không tồn tại \newline (BUG-C-04) & PUT \texttt{/999} & 404 & 200 ``updated'' (0 dòng) & \textcolor{red}{Fail} \\
    TC-C-DT-10 & Xóa danh mục rỗng (không sp) & DELETE danh mục vừa tạo & 200 deleted & 200 & \textcolor{green!60!black}{Pass} \\
    TC-C-DT-11 & Xóa id không tồn tại \newline (BUG-C-04) & DELETE \texttt{/999} & 404 & 200 ``deleted'' & \textcolor{red}{Fail} \\
    TC-C-DT-12 & Xóa danh mục đang có sản phẩm \newline (BUG-C-05) & DELETE \texttt{/1} (2 sp) & Chặn/cascade & 200, sản phẩm mồ côi & \textcolor{red}{Fail} \\
    TC-C-DT-13 & Payload injection ở name & POST \texttt{x'; DROP TABLE\ldots} & Lưu literal an toàn & 200, tham số hóa & \textcolor{green!60!black}{Pass} \\
    TC-C-DT-14 & id không phải số \newline (BUG-C-04) & DELETE \texttt{/abc} & 400/404 & 200 (0 dòng) & \textcolor{orange}{Note} \\
\end{longtable}

\subsection{Bước 5: Ma Trận Truy Vết Độ Phủ (EC $\to$ Test Case)}
Bảng dưới đối chiếu mọi lớp tương đương (Bước~2) với ca kiểm thử phủ nó (kể cả ca BVA). Lớp cùng
kỳ vọng hành vi dùng chung ca đại diện theo nguyên tắc phân hoạch tương đương.

\begin{table}[H]
    \centering
    \caption{Truy vết EC $\to$ TC — Feature C (FR-14)}
    \begin{tabularx}{\linewidth}{C{1.8cm} C{3.4cm} X}
        \toprule
        \textbf{EC} & \textbf{Ca phủ (DT/BVA)} & \textbf{Ghi chú} \\
        \midrule
        EC-NM1 & TC-C-DT-01, BV-06 & Tên mới hợp lệ. \\
        EC-NM2 & TC-C-DT-02, BV-05 & Tên rỗng. \\
        EC-NM3 & TC-C-DT-02 (đại diện) & ``Chỉ khoảng trắng'' cùng kỳ vọng từ chối với tên rỗng (EC-NM2). \\
        EC-NM4 & TC-C-DT-03 & Thiếu trường \texttt{name}. \\
        EC-NM5 & TC-C-DT-04 & Trùng tên đã tồn tại. \\
        EC-NM6 & TC-C-BV-07 & Tên cực dài (biên trên). \\
        EC-NM7 & TC-C-DT-13 & Payload injection. \\
        EC-ID1 & TC-C-DT-08/10, BV-02/03 & id tồn tại (ON min/max). \\
        EC-ID2 & TC-C-DT-09/11, BV-01/04 & id không tồn tại (OFF/OUT). \\
        EC-ID3 & TC-C-DT-14 & id không phải số. \\
        EC-ID4 & TC-C-DT-12 & Danh mục đang có sản phẩm. \\
        EC-AU1 & TC-C-DT-01 & Token admin. \\
        EC-AU2 & TC-C-DT-05 & Token user thường. \\
        EC-AU3 & TC-C-DT-06 & Không có token. \\
        EC-AU4 & TC-C-DT-07 & Token sai/hết hạn. \\
        \bottomrule
    \end{tabularx}
\end{table}
\noindent\textbf{Kết luận độ phủ:} 7/7 lớp \texttt{name}, 4/4 lớp \texttt{id}, 4/4 lớp quyền
truy cập đều có ít nhất một ca kiểm thử.

\section{Boundary Value Analysis (BVA)}

BVA cho FR-14 nhắm vào hai biên: \emph{ranh giới tồn tại} của \texttt{id} (miền hợp lệ $[1,3]$
theo seed) và \texttt{name.length}. Với \texttt{id}, ta kiểm cả hai đầu miền (dưới 1, trên 3) để
lộ lỗi ``thành công giả'' khi id ngoài miền; với \texttt{name.length}, ta kiểm biên rỗng (0/1) và
biên trên (không có max). Bước~3 tổng hợp ma trận phủ biên.

\subsection{Bước 1: Xác Định Ranh Giới}
\begin{table}[H]
    \centering
    \caption{Bảng giá trị biên — Feature C (FR-14)}
    \begin{tabularx}{\linewidth}{L{3.2cm} C{1.8cm} C{1.8cm} C{1.8cm} X}
        \toprule
        \textbf{Biên} & \textbf{Off/dưới} & \textbf{On} & \textbf{Off/trên} & \textbf{Ghi chú} \\
        \midrule
        id tồn tại $[1,3]$ & 0 & 1 và 3 & 4 & id ngoài miền vẫn trả 200 (BUG-C-04). \\
        name.length & 0 (rỗng) & 1 & --- (không max) & length 0 bị nhận (BUG-C-02); không có biên trên (BUG-C-06). \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Bước 2: Thiết Kế Test Cases — BVA}
\begin{longtable}{C{1.2cm} L{3.2cm} L{3.1cm} L{3cm} C{1.9cm} C{1.4cm}}
    \caption{Test cases — BVA, Feature C} \\
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endfirsthead
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endhead
    \bottomrule
    \endlastfoot
    TC-C-BV-01 & id dưới biên \newline (BUG-C-04) & DELETE \texttt{/0} & 404 & 200 ``deleted'' & \textcolor{red}{Fail} \\
    TC-C-BV-02 & id min hợp lệ & PUT \texttt{/1} & 200 updated & 200 & \textcolor{green!60!black}{Pass} \\
    TC-C-BV-03 & id max hợp lệ & PUT \texttt{/3} & 200 updated & 200 & \textcolor{green!60!black}{Pass} \\
    TC-C-BV-04 & id trên biên (max+1) \newline (BUG-C-04) & DELETE \texttt{/4} & 404 & 200 ``deleted'' & \textcolor{red}{Fail} \\
    TC-C-BV-05 & name rỗng (length 0) \newline (BUG-C-02) & POST \texttt{\{name:""\}} & 400 từ chối & 200 tạo được & \textcolor{red}{Fail} \\
    TC-C-BV-06 & name length 1 & POST \texttt{\{name:"A"\}} & 200 & 200 & \textcolor{green!60!black}{Pass} \\
    TC-C-BV-07 & name cực dài \newline (BUG-C-06) & POST name 100.000 ký tự & Nên giới hạn/từ chối & 200 lưu toàn bộ & \textcolor{red}{Fail} \\
\end{longtable}

\subsection{Bước 3: Ma Trận Phủ Biên (off / on / on$+1$)}
Bảng dưới chứng minh mỗi biên ở Bước~1 đều được kiểm đủ, lấy ca từ cả bảng BVA lẫn Domain Testing.

\begin{table}[H]
    \centering
    \caption{Phủ biên — Feature C (FR-14)}
    \begin{tabularx}{\linewidth}{L{2.8cm} C{1.7cm} C{2.4cm} C{1.6cm} X}
        \toprule
        \textbf{Biên} & \textbf{Dưới} & \textbf{On} & \textbf{Trên} & \textbf{Ngoài / Ghi chú} \\
        \midrule
        id tồn tại $[1,3]$ & BV-01 (0) & BV-02 (1), BV-03 (3) & BV-04 (4) & Ngoài miền: DT-09/11 (999). Mọi điểm off/out đều trả 200 (BUG-C-04). \\
        name.length $\ge 1$ & BV-05 (0) & BV-06 (1) & --- (không max) & Biên trên khuyết: BV-07 (100k). \\
        \bottomrule
    \end{tabularx}
\end{table}
\noindent\textbf{Kết luận:} biên tồn tại của \texttt{id} phủ cả hai đầu miền + ngoài miền (đều
lộ ``thành công giả''); \texttt{name.length} phủ rỗng/1 và biên trên khuyết.

\section{AI Gap Analysis}
\begin{table}[H]
    \centering
    \caption{AI Gap Analysis — Feature C (FR-14)}
    \begin{tabularx}{\linewidth}{C{0.5cm} X L{4cm} C{1.8cm}}
        \toprule
        \textbf{\#} & \textbf{Ca/bug bị bỏ sót} & \textbf{Lý do bỏ sót} & \textbf{Bổ sung ở TC} \\
        \midrule
        1 & Thiếu kiểm soát truy cập theo vai trò (admin) — user thường CRUD được danh mục. & Giới hạn công cụ (AI tập trung dữ liệu đầu vào, bỏ sót tầng phân quyền/middleware). & TC-C-DT-05 \\
        2 & PUT/DELETE id không tồn tại trả 200 (không kiểm \texttt{this.changes}) — ``thành công giả''. & Chất lượng prompt (chỉ thử happy path) + biến \emph{đầu ra/trạng thái}. & TC-C-DT-09/11, BV-01/04 \\
        3 & Xóa danh mục làm mồ côi sản phẩm (không khóa ngoại/cascade). & Độ phức tạp nội tại (toàn vẹn tham chiếu, tương tác đa bảng). & TC-C-DT-12 \\
        4 & Trùng tên danh mục (không \texttt{UNIQUE}). & Chất lượng prompt (cần đọc kỹ ràng buộc lược đồ). & TC-C-DT-04 \\
        5 & Tên rỗng/NULL được chấp nhận (backend không kiểm). & Độ phức tạp (validation tách tầng UI/API). & TC-C-DT-02/03, BV-05 \\
        6 & Không giới hạn độ dài trên của \texttt{name}. & Độ phức tạp/robustness; BVA thường chỉ nhìn biên đã đặc tả. & TC-C-BV-07 \\
        \bottomrule
    \end{tabularx}
\end{table}

\section{Bug Reports — Feature C}
Năm lỗi ứng viên dưới đây suy ra từ phân tích mã nguồn; cần \textbf{xác nhận bằng thực thi},
chụp màn hình và tạo GitHub Issue trước khi nộp.

\subsection{BUG-C-01: CRUD danh mục không giới hạn quyền admin}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-C-01 \\
        Tính năng    & FR-14 \quad\textbf{Kỹ thuật:} Domain \\
        Mức độ       & Critical \\
        Mô tả        & \texttt{authenticateToken} chỉ kiểm JWT hợp lệ, không kiểm \texttt{role="admin"}; mọi tài khoản đăng nhập đều tạo/sửa/xóa danh mục. Kèm theo, \texttt{PUT /api/users/me} cho phép tự đặt \texttt{role} $\Rightarrow$ tự nâng quyền. \\
        Tái hiện     & 1. Đăng nhập tài khoản \texttt{role='user'}. \newline 2. Gọi \texttt{POST /api/categories} với token đó. \newline 3. Quan sát danh mục được tạo. \\
        Expected     & \texttt{403 Forbidden} cho tài khoản không phải admin. \\
        Actual       & \texttt{200 ``Category created''} (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_c_01.png}\caption{Screenshot — BUG-C-01}\end{figure}

\subsection{BUG-C-02: Không kiểm tra tên danh mục (rỗng/NULL được chấp nhận)}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-C-02 \\
        Tính năng    & FR-14 \quad\textbf{Kỹ thuật:} Domain + BVA \\
        Mức độ       & Major \\
        Mô tả        & \texttt{POST/PUT} không kiểm \texttt{name}; tên rỗng, chỉ khoảng trắng, hoặc thiếu trường (\texttt{NULL}) đều được lưu. \\
        Tái hiện     & 1. \texttt{POST /api/categories} với body \texttt{\{name:""\}} hoặc \texttt{\{\}}. \\
        Expected     & \texttt{400 Bad Request}. \\
        Actual       & \texttt{200 ``Category created''}, tạo danh mục rỗng/NULL (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_c_02.png}\caption{Screenshot — BUG-C-02}\end{figure}

\subsection{BUG-C-03: Cho phép tên danh mục trùng lặp}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-C-03 \\
        Tính năng    & FR-14 \quad\textbf{Kỹ thuật:} Domain \\
        Mức độ       & Minor \\
        Mô tả        & Cột \texttt{name} không \texttt{UNIQUE}; tạo trùng tên danh mục đã tồn tại vẫn thành công, gây nhập nhằng khi lọc sản phẩm. \\
        Tái hiện     & 1. \texttt{POST /api/categories} với \texttt{\{name:"Laptop"\}} (đã có trong seed). \\
        Expected     & Từ chối vì tên đã tồn tại. \\
        Actual       & Tạo thêm danh mục ``Laptop'' thứ hai (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_c_03.png}\caption{Screenshot — BUG-C-03}\end{figure}

\subsection{BUG-C-04: Sửa/xóa id không tồn tại vẫn trả về thành công}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-C-04 \\
        Tính năng    & FR-14 \quad\textbf{Kỹ thuật:} BVA \\
        Mức độ       & Minor \\
        Mô tả        & \texttt{PUT/DELETE} không kiểm \texttt{this.changes}; với \texttt{id} không tồn tại (0, 4, 999) hoặc không phải số, \texttt{UPDATE/DELETE} tác động 0 dòng nhưng vẫn trả \texttt{200 ``updated/deleted''}. \\
        Tái hiện     & 1. \texttt{DELETE /api/categories/999} (hoặc \texttt{/0}, \texttt{/abc}). \\
        Expected     & \texttt{404 Not Found}. \\
        Actual       & \texttt{200 ``Category deleted''} dù không có gì bị xóa (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_c_04.png}\caption{Screenshot — BUG-C-04}\end{figure}

\subsection{BUG-C-05: Xóa danh mục làm mồ côi sản phẩm (mất toàn vẹn tham chiếu)}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-C-05 \\
        Tính năng    & FR-14 \quad\textbf{Kỹ thuật:} Domain \\
        Mức độ       & Major \\
        Mô tả        & \texttt{products.category\_id} không có khóa ngoại tới \texttt{categories}; xóa một danh mục đang được sản phẩm tham chiếu để lại \texttt{category\_id} trỏ tới danh mục không còn tồn tại (sản phẩm mồ côi), không cascade, không chặn. \\
        Tái hiện     & 1. \texttt{DELETE /api/categories/1} (danh mục ``Điện thoại'' có 2 sản phẩm). \newline 2. \texttt{GET /api/products} kiểm tra \texttt{category\_id=1}. \\
        Expected     & Chặn xóa khi còn sản phẩm, hoặc cascade/di chuyển sản phẩm. \\
        Actual       & Xóa thành công, 2 sản phẩm còn \texttt{category\_id=1} trỏ tới danh mục đã mất (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_c_05.png}\caption{Screenshot — BUG-C-05}\end{figure}

% ============================================================
%  CHƯƠNG 5: FEATURE D — POOL D (MOBILE)
% ============================================================
\chapter{Feature D — Giỏ hàng (Pool D — Mobile)}

\section{Mô Tả Tính Năng}
Tính năng \textbf{FR-07 — Giỏ hàng} trên app \emph{mobile} (React Native/Expo) cho phép thêm
sản phẩm vào giỏ, chỉnh số lượng, xóa, rồi thanh toán. Toàn bộ logic nằm trong
\texttt{frontend-mobile/App.js} (giỏ hàng là \emph{trạng thái phía client}, chưa gọi API cho
tới khi checkout).

\begin{itemize}
    \item \texttt{normalizeQuantity(v)} — \texttt{parseInt}; trả về giá trị nếu hữu hạn và
    $>0$, ngược lại trả \texttt{1}.
    \item \texttt{addToCart(product, qty)} — nếu sản phẩm đã có trong giỏ thì cộng dồn số
    lượng; ngược lại thêm mục mới; luôn hiện \texttt{Alert} ``Đã thêm''.
    \item \textbf{Ô sửa số lượng trong giỏ} — \texttt{onChangeText} đặt
    \texttt{quantity = (parsed>0) ? parsed + 1 : 1}.
    \item \texttt{cartTotal} $= \sum(\texttt{price}\times\texttt{quantity})$.
    \item \texttt{handleConfirmCheckout} — gửi \verb|items = cart.length > 1 ? cart.slice(0,-1) : cart|
    và \texttt{total\_amount = final\_amount ?? cartTotal}.
\end{itemize}

\vspace{4pt}
\noindent\textbf{Trích mã (rút gọn):}
\begin{lstlisting}[caption={Giỏ hàng — App.js (rút gọn)},numbers=none]
// (A) ep so luong hop le
const normalizeQuantity = (value) => {
  const parsed = parseInt(value, 10);
  return Number.isFinite(parsed) && parsed > 0 ? parsed : 1;
};
// (B) o sua so luong trong gio -> luu parsed + 1
newCart[index].quantity = (Number.isFinite(parsed) && parsed > 0) ? parsed + 1 : 1;
// (C) checkout bo mat mon cuoi khi gio > 1 mon
items: cart.length > 1 ? cart.slice(0, -1) : cart,
total_amount: finalAmount,   // = cartTotal (tinh du moi mon)
\end{lstlisting}

\begin{notebox}
Khiếm khuyết trọng tâm: \textbf{(B)} sửa số lượng thành $N$ thì giỏ lưu $N+1$ (off-by-one, thổi
phồng số lượng mỗi lần sửa); \textbf{(C)} khi giỏ có $>1$ món, \texttt{slice(0,-1)} \emph{bỏ mất
món cuối} khỏi đơn nhưng \texttt{total\_amount} vẫn tính đủ mọi món $\Rightarrow$ khách bị tính
tiền món không được ghi nhận. Ngoài ra \textbf{(A)} âm thầm ép mọi số lượng không hợp lệ
(0/âm/chữ/thập phân) về 1.
\end{notebox}

\begin{notebox}
\textbf{Quy ước cột kết quả} (như Feature A/B/C): cột \emph{Kết quả}/\emph{Trạng thái} là kết
quả \textbf{thực thi thật} (2026-07-02) bằng cách chạy \emph{mô phỏng đúng logic}
(\texttt{normalizeQuantity}, handler ô sửa số lượng, \texttt{slice(0,-1)}) trích nguyên văn từ
\texttt{frontend-mobile/App.js} trong Node; mọi kết quả khớp dự đoán. Ảnh chụp trên thiết bị
cần chạy app Expo.
\end{notebox}

\section{Domain Testing}

Áp dụng Domain Testing theo bốn bước của bài giảng (\emph{xác định biến $\to$ phân hoạch miền
$\to$ chọn điểm ON/OFF/IN/OUT $\to$ thiết kế ca}). Với FR-07, hai biên trọng tâm là
\texttt{quantity} (ranh giới $>0$, phân biệt \emph{thêm vào giỏ} với \emph{ô sửa trong giỏ}) và
\texttt{cart.length} (tại 1 --- nơi \texttt{slice(0,-1)} bắt đầu bỏ mất món cuối). Biến đầu ra
\texttt{cartTotal}/\texttt{total\_amount} được đưa vào để lộ mâu thuẫn ``đơn thiếu món nhưng tính
đủ tiền''. Bước~5 tổng hợp ma trận truy vết EC $\to$ TC.

\subsection{Bước 1: Xác Định Các Biến Đầu Vào}
\begin{table}[H]
    \centering
    \caption{Danh sách biến — Feature D (FR-07 Mobile)}
    \begin{tabularx}{\linewidth}{C{0.5cm} L{3.2cm} L{2.6cm} C{1.8cm} X}
        \toprule
        \textbf{\#} & \textbf{Biến} & \textbf{Kiểu} & \textbf{Vào/Ra} & \textbf{Ghi chú} \\
        \midrule
        1 & quantity (trang chi tiết) & string$\to$int & Input & Biên $>0$; \texttt{parseInt} + ép về 1. \\
        2 & quantity (ô sửa trong giỏ) & string$\to$int & Input & Handler lưu \texttt{parsed+1} (lỗi). \\
        3 & cart.length & integer & Trạng thái & Biên tại 1: $>1$ kích hoạt \texttt{slice(0,-1)}. \\
        4 & product.id & integer & Input & Trùng id $\Rightarrow$ cộng dồn số lượng. \\
        5 & cartTotal / total\_amount & integer & \textbf{Output} & Tính đủ mọi món dù đơn thiếu món. \\
        6 & stock / max qty & --- & (thiếu) & Không có mô hình tồn kho/giới hạn. \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Bước 2: Phân Hoạch Miền}
Dấu \emph{*} = lớp mà hệ thống \emph{đúng} phải xử lý nhưng mã hiện tại làm sai.

\subsubsection{Biến quantity (thêm vào giỏ)}
\begin{table}[H]
    \centering
    \caption{Phân hoạch miền — quantity (addToCart)}
    \begin{tabularx}{\linewidth}{C{1.4cm} L{6cm} C{1.6cm} X}
        \toprule
        \textbf{EC} & \textbf{Điều kiện} & \textbf{Loại} & \textbf{Ví dụ} \\
        \midrule
        EC-Q1 & Số nguyên dương & Valid & \texttt{3} \\
        EC-Q2 & Mặc định 1 & Valid & \texttt{1} \\
        EC-Q3 & Bằng 0 & Invalid* & \texttt{0} $\to$ ép 1 \\
        EC-Q4 & Âm & Invalid* & \texttt{-2} $\to$ ép 1 \\
        EC-Q5 & Không phải số & Invalid* & \texttt{abc} $\to$ ép 1 \\
        EC-Q6 & Thập phân & Invalid* & \texttt{2.9} $\to$ 2 (cắt) \\
        EC-Q7 & Rất lớn (không có tồn kho/max) & Valid* & \texttt{999999} \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsubsection{Biến quantity (ô sửa trong giỏ) \& cart.length}
\begin{table}[H]
    \centering
    \caption{Phân hoạch miền — sửa số lượng \& số món trong giỏ}
    \begin{tabularx}{\linewidth}{C{1.4cm} L{6.5cm} C{1.6cm} X}
        \toprule
        \textbf{EC} & \textbf{Điều kiện} & \textbf{Loại} & \textbf{Kỳ vọng} \\
        \midrule
        EC-ED1 & Sửa thành $N>0$ & Invalid* & \emph{Nên} lưu $N$; code lưu $N+1$ \\
        EC-ED2 & Sửa thành 0/không hợp lệ & Invalid* & Reset về 1 (âm thầm) \\
        EC-L1 & Giỏ có 0 món & Boundary & Không cho checkout \\
        EC-L2 & Giỏ có 1 món & Valid & Gửi đủ 1 món \\
        EC-L3 & Giỏ có $>1$ món & Invalid* & \emph{Bỏ mất} món cuối (slice) \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Bước 3: Chọn Điểm Kiểm Thử (ON/OFF/IN/OUT)}
Hai biên đáng chú ý: \texttt{quantity} tại ranh giới $>0$ và \texttt{cart.length} tại ranh giới
$1$ (nơi \texttt{slice(0,-1)} bắt đầu bỏ món).

\begin{table}[H]
    \centering
    \caption{Điểm ON/OFF/IN — quantity ($>0$) và cart.length}
    \begin{tabularx}{\linewidth}{L{3.4cm} C{2cm} X}
        \toprule
        \textbf{Biên} & \textbf{Giá trị} & \textbf{Kỳ vọng (đặc tả)} \\
        \midrule
        quantity OFF   & 0 & Từ chối/giữ nguyên (code ép 1) \\
        quantity ON    & 1 & Chấp nhận, số lượng = 1 \\
        quantity IN    & 2 & Chấp nhận, số lượng = 2 \\
        cart.length ON & 1 & Gửi đủ 1 món \\
        cart.length +1 & 2 & Gửi đủ 2 món (code bỏ 1) \\
        \bottomrule
    \end{tabularx}
\end{table}

\noindent\textbf{Nhận xét:} biên \texttt{addToCart} xử lý số lượng \emph{tương đối} ổn (ép về 1),
nhưng ô \emph{sửa} trong giỏ lại cộng thêm 1 (BUG-D-01); và biên \texttt{cart.length=1$\to$2}
làm lộ lỗi bỏ món cuối (BUG-D-02).

\subsection{Bước 4: Thiết Kế Test Cases — Domain Testing}
\begin{longtable}{C{1.2cm} L{3.2cm} L{3.1cm} L{3cm} C{1.9cm} C{1.4cm}}
    \caption{Test cases — Domain Testing, Feature D} \\
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endfirsthead
    \multicolumn{6}{c}{\small\itshape (tiếp theo)} \\
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endhead
    \midrule
    \multicolumn{6}{r}{\small\itshape (còn tiếp)} \\
    \endfoot
    \bottomrule
    \endlastfoot
    TC-D-DT-01 & Thêm với số lượng 3 & qty=\texttt{"3"} & Giỏ có món, qty=3 & qty=3 & \textcolor{green!60!black}{Pass} \\
    TC-D-DT-02 & Thêm số lượng 0 \newline (BUG-D-03) & qty=\texttt{"0"} & Từ chối/giữ 0 & Âm thầm ép qty=1 & \textcolor{orange}{Note} \\
    TC-D-DT-03 & Thêm số lượng âm \newline (BUG-D-03) & qty=\texttt{"-2"} & Từ chối & Ép qty=1 & \textcolor{orange}{Note} \\
    TC-D-DT-04 & Thêm số lượng chữ \newline (BUG-D-03) & qty=\texttt{"abc"} & Từ chối & Ép qty=1 & \textcolor{orange}{Note} \\
    TC-D-DT-05 & Thêm số lượng thập phân \newline (BUG-D-03) & qty=\texttt{"2.9"} & Từ chối/làm tròn rõ ràng & \texttt{parseInt}$\to$2 (cắt) & \textcolor{orange}{Note} \\
    TC-D-DT-06 & Thêm trùng sản phẩm (2 rồi 3) & cùng id & Cộng dồn = 5 & 5 & \textcolor{green!60!black}{Pass} \\
    TC-D-DT-07 & Sửa số lượng trong giỏ thành 2 \newline (BUG-D-01) & ô sửa = \texttt{2} & qty=2 & qty=3 (parsed+1) & \textcolor{red}{Fail} \\
    TC-D-DT-08 & Sửa số lượng trong giỏ thành 5 \newline (BUG-D-01) & ô sửa = \texttt{5} & qty=5 & qty=6 & \textcolor{red}{Fail} \\
    TC-D-DT-09 & Sửa thành giá trị không hợp lệ & ô sửa = \texttt{"x"} & Giữ nguyên/báo lỗi & Reset về 1 & \textcolor{orange}{Note} \\
    TC-D-DT-10 & Xóa một món khỏi giỏ & bấm ``Xóa'' & Món bị xóa & Bị xóa & \textcolor{green!60!black}{Pass} \\
    TC-D-DT-11 & Checkout giỏ 1 món & 1 món & Đơn gồm đúng 1 món & Đúng & \textcolor{green!60!black}{Pass} \\
    TC-D-DT-12 & Checkout giỏ 3 món \newline (BUG-D-02) & 3 món & Đơn gồm 3 món, tổng khớp & Đơn chỉ 2 món, tính đủ 3 & \textcolor{red}{Fail} \\
    TC-D-DT-13 & Thêm số lượng cực lớn \newline (BUG-D-04) & qty=\texttt{"999999"} & Chặn theo tồn kho/max & Chấp nhận & \textcolor{red}{Fail} \\
    TC-D-DT-14 & Checkout giỏ rỗng & 0 món & Chặn (không cho checkout) & Vào màn checkout & \textcolor{orange}{Note} \\
\end{longtable}

\subsection{Bước 5: Ma Trận Truy Vết Độ Phủ (EC $\to$ Test Case)}
Bảng dưới đối chiếu mọi lớp tương đương (Bước~2) với ca kiểm thử phủ nó (kể cả ca BVA), chứng
minh độ phủ đầy đủ cho cả hai nhóm biên \texttt{quantity} và \texttt{cart.length}.

\begin{table}[H]
    \centering
    \caption{Truy vết EC $\to$ TC — Feature D (FR-07 Mobile)}
    \begin{tabularx}{\linewidth}{C{1.8cm} C{3.4cm} X}
        \toprule
        \textbf{EC} & \textbf{Ca phủ (DT/BVA)} & \textbf{Ghi chú} \\
        \midrule
        EC-Q1 & TC-D-DT-01, BV-03 & Số nguyên dương. \\
        EC-Q2 & TC-D-BV-02 & Mặc định 1 (đúng biên). \\
        EC-Q3 & TC-D-DT-02, BV-01 & Bằng 0 $\to$ ép 1. \\
        EC-Q4 & TC-D-DT-03 & Âm $\to$ ép 1. \\
        EC-Q5 & TC-D-DT-04 & Không phải số $\to$ ép 1. \\
        EC-Q6 & TC-D-DT-05 & Thập phân $\to$ cắt phần nguyên. \\
        EC-Q7 & TC-D-DT-13, BV-07 & Rất lớn (không có tồn kho/max). \\
        EC-ED1 & TC-D-DT-07/08, BV-04 & Ô sửa lưu \texttt{parsed+1} (off-by-one). \\
        EC-ED2 & TC-D-DT-09 & Ô sửa giá trị không hợp lệ $\to$ reset 1. \\
        EC-L1 & TC-D-DT-14 & Giỏ 0 món. \\
        EC-L2 & TC-D-DT-11, BV-05 & Giỏ 1 món (gửi đủ). \\
        EC-L3 & TC-D-DT-12, BV-06 & Giỏ $>1$ món (bỏ mất món cuối). \\
        \bottomrule
    \end{tabularx}
\end{table}
\noindent\textbf{Kết luận độ phủ:} 7/7 lớp \texttt{quantity}, 2/2 lớp ô sửa, 3/3 lớp
\texttt{cart.length} đều có ít nhất một ca kiểm thử --- phủ 100\%.

\section{Boundary Value Analysis (BVA)}

BVA cho FR-07 nhắm vào ba biên số lượng / trạng thái: \texttt{quantity} khi \emph{thêm} vào giỏ
($>0$), \texttt{quantity} ở \emph{ô sửa trong giỏ} (lưu \texttt{parsed$+1$}), và
\texttt{cart.length} (tại 1). Mỗi biên được kiểm bộ ba \texttt{off/on/on$+1$}; đặc biệt ô sửa lộ
off-by-one (nhập 1 thành 2), còn \texttt{cart.length}$=2$ lộ lỗi bỏ mất món cuối. Bước~3 tổng hợp
ma trận phủ biên.

\subsection{Bước 1: Xác Định Ranh Giới}
\begin{table}[H]
    \centering
    \caption{Bảng giá trị biên — Feature D (FR-07 Mobile)}
    \begin{tabularx}{\linewidth}{L{3.4cm} C{1.8cm} C{1.8cm} C{1.8cm} X}
        \toprule
        \textbf{Biên} & \textbf{Off/dưới} & \textbf{On} & \textbf{On+1} & \textbf{Ghi chú} \\
        \midrule
        quantity $>0$ (thêm) & 0 & 1 & 2 & 0 bị ép về 1; không có biên trên (thiếu tồn kho). \\
        quantity (ô sửa giỏ) & 0/invalid & 1 & 2 & Lưu $N+1$: nhập 1$\to$2, 2$\to$3 (BUG-D-01). \\
        cart.length & 0 & 1 & 2 & $\ge 2$ bỏ mất món cuối (BUG-D-02). \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Bước 2: Thiết Kế Test Cases — BVA}
\begin{longtable}{C{1.2cm} L{3.2cm} L{3.1cm} L{3cm} C{1.9cm} C{1.4cm}}
    \caption{Test cases — BVA, Feature D} \\
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endfirsthead
    \toprule
    \textbf{TC\#} & \textbf{Mô tả} & \textbf{Input} & \textbf{Expected Output} & \textbf{Kết quả} & \textbf{Trạng thái} \\
    \midrule
    \endhead
    \bottomrule
    \endlastfoot
    TC-D-BV-01 & qty dưới biên (0) \newline (BUG-D-03) & thêm qty=\texttt{"0"} & Từ chối & Ép về 1 & \textcolor{orange}{Note} \\
    TC-D-BV-02 & qty đúng biên (1) & thêm qty=\texttt{"1"} & qty=1 & qty=1 & \textcolor{green!60!black}{Pass} \\
    TC-D-BV-03 & qty trên biên (2) & thêm qty=\texttt{"2"} & qty=2 & qty=2 & \textcolor{green!60!black}{Pass} \\
    TC-D-BV-04 & Ô sửa giỏ nhập 1 \newline (BUG-D-01) & ô sửa=\texttt{1} & qty=1 & qty=2 (parsed+1) & \textcolor{red}{Fail} \\
    TC-D-BV-05 & cart.length=1 & 1 món checkout & Đơn đủ 1 món & Đúng & \textcolor{green!60!black}{Pass} \\
    TC-D-BV-06 & cart.length=2 \newline (BUG-D-02) & 2 món checkout & Đơn đủ 2 món & Đơn chỉ 1 món & \textcolor{red}{Fail} \\
    TC-D-BV-07 & qty biên trên (không max) \newline (BUG-D-04) & qty=\texttt{"999999"} & Chặn/giới hạn & Chấp nhận & \textcolor{red}{Fail} \\
\end{longtable}

\subsection{Bước 3: Ma Trận Phủ Biên (off / on / on$+1$)}
Bảng dưới chứng minh mỗi biên ở Bước~1 đều được kiểm đủ ba giá trị, lấy ca từ cả bảng BVA lẫn
Domain Testing.

\begin{table}[H]
    \centering
    \caption{Phủ biên — Feature D (FR-07 Mobile)}
    \begin{tabularx}{\linewidth}{L{2.8cm} C{1.9cm} C{1.7cm} C{1.6cm} X}
        \toprule
        \textbf{Biên} & \textbf{Off} & \textbf{On} & \textbf{On$+1$} & \textbf{Ngoài / Ghi chú} \\
        \midrule
        quantity $>0$ (thêm) & BV-01 (0) & BV-02 (1) & BV-03 (2) & Không có max $\Rightarrow$ BV-07 (999999). \\
        quantity (ô sửa giỏ) & DT-09 (0/invalid) & BV-04 (1$\to$2) & DT-07 (2$\to$3) & Lộ off-by-one \texttt{parsed$+1$} (BUG-D-01). \\
        cart.length & DT-14 (0) & BV-05 (1) & BV-06 (2) & On$+1$ kích hoạt \texttt{slice(0,-1)} bỏ món cuối (BUG-D-02). \\
        \bottomrule
    \end{tabularx}
\end{table}
\noindent\textbf{Kết luận:} ba biên đều phủ đủ off/on/on$+1$; hai biên \emph{ô sửa} và
\texttt{cart.length} chính là nơi hai lỗi nghiêm trọng (BUG-D-01, BUG-D-02) biểu hiện.

\section{AI Gap Analysis}
\begin{table}[H]
    \centering
    \caption{AI Gap Analysis — Feature D (FR-07 Mobile)}
    \begin{tabularx}{\linewidth}{C{0.5cm} X L{4cm} C{1.8cm}}
        \toprule
        \textbf{\#} & \textbf{Ca/bug bị bỏ sót} & \textbf{Lý do bỏ sót} & \textbf{Bổ sung ở TC} \\
        \midrule
        1 & Ô sửa số lượng lưu \texttt{parsed+1} — cần đọc \emph{đúng phép tính} trong handler, không chỉ ``cập nhật số lượng''. & Giới hạn công cụ + độ phức tạp (logic UI mobile). & TC-D-DT-07/08, BV-04 \\
        2 & Checkout bỏ mất món cuối (\texttt{slice(0,-1)}) trong khi tổng tiền tính đủ. & Độ phức tạp (biến \emph{đầu ra}, chỉ lộ khi giỏ $>1$ món — trạng thái). & TC-D-DT-12, BV-06 \\
        3 & Ép âm thầm số lượng 0/âm/chữ/thập phân về 1. & Chất lượng prompt (dễ coi là ``đã xử lý'' thay vì lỗi UX). & TC-D-DT-02..05 \\
        4 & Không có giới hạn số lượng/tồn kho (biên trên). & Độ phức tạp/robustness; BVA thường chỉ nhìn biên đã đặc tả. & TC-D-DT-13, BV-07 \\
        5 & Lỗi phụ thuộc \emph{trạng thái nhiều món} (cart.length$>1$) — khó phát hiện nếu chỉ thử 1 món. & Độ phức tạp nội tại (tương tác trạng thái). & TC-D-DT-12 \\
        \bottomrule
    \end{tabularx}
\end{table}

\section{Bug Reports — Feature D}
Bốn lỗi ứng viên dưới đây suy ra từ phân tích mã nguồn mobile; cần \textbf{xác nhận bằng thực
thi trên app}, chụp màn hình và tạo GitHub Issue trước khi nộp.
\subsection{BUG-D-01: Sửa số lượng trong giỏ bị cộng thêm 1 (off-by-one)}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-D-01 \\
        Tính năng    & FR-07 (Mobile) \quad\textbf{Kỹ thuật:} Domain + BVA \\
        Mức độ       & Major \\
        Mô tả        & Handler \texttt{onChangeText} của ô số lượng trong giỏ đặt \texttt{quantity = parsed + 1}; nhập $N$ thì lưu $N+1$, làm sai số lượng và tổng tiền mỗi lần sửa. \\
        Tái hiện     & 1. Thêm 1 sản phẩm vào giỏ. \newline 2. Mở giỏ, sửa ô số lượng thành \texttt{2}. \newline 3. Quan sát số lượng/thành tiền. \\
        Expected     & Số lượng = 2. \\
        Actual       & Số lượng = 3 (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_d_01.png}\caption{Screenshot — BUG-D-01}\end{figure}

\subsection{BUG-D-02: Checkout bỏ mất món cuối nhưng vẫn tính đủ tiền}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-D-02 \\
        Tính năng    & FR-07 (Mobile) \quad\textbf{Kỹ thuật:} Domain + BVA \\
        Mức độ       & Major \\
        Mô tả        & \texttt{handleConfirmCheckout} gửi \verb|items = cart.length > 1 ? cart.slice(0,-1) : cart| — bỏ món cuối khi giỏ có $>1$ món, trong khi \texttt{total\_amount} vẫn bằng \texttt{cartTotal} (tính đủ mọi món). Đơn ghi thiếu món nhưng khách trả đủ tiền. \\
        Tái hiện     & 1. Thêm 3 sản phẩm khác nhau vào giỏ. \newline 2. Thanh toán. \newline 3. Xem đơn ở lịch sử: chỉ 2 món, tổng tính cho 3. \\
        Expected     & Đơn gồm đủ 3 món, tổng tiền khớp danh sách món. \\
        Actual       & Đơn chỉ 2 món, tổng tính đủ 3 (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_d_02.png}\caption{Screenshot — BUG-D-02}\end{figure}

\subsection{BUG-D-03: Số lượng không hợp lệ bị âm thầm ép về 1}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-D-03 \\
        Tính năng    & FR-07 (Mobile) \quad\textbf{Kỹ thuật:} Domain \\
        Mức độ       & Minor \\
        Mô tả        & \texttt{normalizeQuantity} dùng \texttt{parseInt} và ép mọi giá trị không hợp lệ (0, âm, chữ) về \texttt{1}; số thập phân bị cắt (\texttt{"2.9"}$\to$2) — không báo cho người dùng biết đầu vào đã bị thay đổi. \\
        Tái hiện     & 1. Trang chi tiết, nhập số lượng \texttt{0} (hoặc \texttt{-2}, \texttt{abc}, \texttt{2.9}). \newline 2. Thêm vào giỏ. \\
        Expected     & Báo lỗi/không cho thêm với số lượng không hợp lệ. \\
        Actual       & Âm thầm thêm với số lượng 1 (hoặc phần nguyên) (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_d_03.png}\caption{Screenshot — BUG-D-03}\end{figure}

\subsection{BUG-D-04: Không giới hạn số lượng / không kiểm tồn kho}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Bug ID       & BUG-D-04 \\
        Tính năng    & FR-07 (Mobile) \quad\textbf{Kỹ thuật:} BVA \\
        Mức độ       & Minor \\
        Mô tả        & Không có biên trên cho số lượng và không có mô hình tồn kho; có thể thêm số lượng cực lớn (vd \texttt{999999}) vào giỏ, tạo tổng tiền phi lý. \\
        Tái hiện     & 1. Trang chi tiết, nhập số lượng \texttt{999999}. \newline 2. Thêm vào giỏ, xem tổng. \\
        Expected     & Giới hạn theo tồn kho / mức tối đa hợp lý. \\
        Actual       & Chấp nhận toàn bộ, tổng tiền cực lớn (xác nhận bằng thực thi API, 2026-07-02). \\
        GitHub Issue & \url{https://github.com/[group]/eshop-sut/issues/[number]} \\
        \bottomrule
    \end{tabularx}
\end{table}
% \begin{figure}[H]\centering\includegraphics[width=0.8\linewidth]{bug_d_04.png}\caption{Screenshot — BUG-D-04}\end{figure}

% ============================================================
%  CHƯƠNG 6: AGENT SKILL
% ============================================================
\chapter{Agent Skill}

\section{Mô Tả Skill}
Em xây dựng một \textbf{Agent Skill} tên \texttt{domain-bva-test} (đặt tại
\texttt{.claude/skills/domain-bva-test/SKILL.md}) chạy trong Claude Code. Skill hiện thực hóa
đúng \textbf{Mục 6} của đề bài: dẫn AI đi qua \emph{từng bước} của Domain Testing và Boundary
Value Analysis thay vì một prompt chung chung ``sinh test case và tìm bug''.

\subsection{Nguyên tắc cốt lõi}
\begin{itemize}
    \item \textbf{Disciplined AI-First:} skill ép AI thực hiện tuần tự 10 bước, mỗi bước là một
    hạng mục kỹ thuật có thể review được (không nhảy thẳng tới bảng kết quả).
    \item \textbf{Human review:} sau mỗi bước, sinh viên đối chiếu với mã nguồn/đặc tả; skill
    cấm bịa giá trị đầu vào, Expected hay kết quả thực thi.
    \item \textbf{Truy vết nguồn:} mọi Expected phải bắt nguồn từ đặc tả; mọi ``Actual'' phải
    từ thực thi thật (API hoặc mô phỏng logic), không đoán mò.
\end{itemize}

\subsection{Mười bước của skill}
\begin{enumerate}
    \item \textbf{Identify variables} --- liệt kê mọi biến vào/ra, kể cả biến ẩn (độ dài, số
    món, ngày, cờ boolean).
    \item \textbf{Characterise domain} --- xác định miền hợp lệ + ràng buộc từng biến.
    \item \textbf{Equivalence partitioning} --- chia lớp valid/invalid, mỗi lớp một mã EC.
    \item \textbf{Identify boundaries} --- đánh dấu biên cho biến có thứ tự/độ dài.
    \item \textbf{Domain matrix} --- phân tích ON/OFF/IN/OUT cho mỗi biên.
    \item \textbf{3-value BVA} --- \texttt{min$-$1, min, min$+$1} và \texttt{max$-$1, max, max$+$1}.
    \item \textbf{Design test cases} --- kết hợp phân hoạch + biên, giả định đơn lỗi + vài ca
    tương tác đa biến.
    \item \textbf{AI gap analysis} --- soi lại ca AI bỏ sót; ghi \emph{lý do} (prompt/tool/
    độ phức tạp).
    \item \textbf{Emit report} --- xuất báo cáo có mục cho từng bước (thể hiện suy luận, không
    chỉ bảng cuối).
    \item \textbf{Hand off} --- chỉ điền Actual/pass-fail sau khi thực thi; lỗi $\to$ Bug
    Reporter; ghi AI Audit.
\end{enumerate}

\section{Cách Sử Dụng}
Gọi skill trong Claude Code bằng cú pháp slash-command, truyền feature cần kiểm thử:
\begin{lstlisting}[language=bash, caption={Kích hoạt skill trong Claude Code}]
# Trong phiên Claude Code tại thư mục repo:
/domain-bva-test Áp dụng Domain Testing + BVA cho FR-09 (Mã giảm giá),
đọc backend/server.js và database.js, điền trực tiếp vào main.tex (tiếng Việt),
chế độ "sinh viên tự chạy, AI dự đoán rồi xác nhận bằng thực thi".
\end{lstlisting}

\noindent Metadata của skill (\texttt{SKILL.md}) khai báo điều kiện kích hoạt tự động:
\begin{lstlisting}[caption={Frontmatter của SKILL.md},numbers=none]
---
name: domain-bva-test
description: Apply Domain Testing and Boundary Value Analysis to an
  EShop feature (FR-XX), guiding the AI through every step of the
  technique and producing a reviewable test-case report plus an AI
  gap analysis. Use when the user wants to design test cases for a
  feature, run domain testing / boundary value analysis, or complete
  Section 6 of HW02.
---
\end{lstlisting}

\noindent Skill này đã được dùng để tạo toàn bộ 4 chương Feature A/B/C/D trong báo cáo này
(xem Phụ lục B --- AI Audit, Interaction 1--4).

\section{Video Demo}
\begin{itemize}
    \item \textbf{Video 1:} \url{https://youtube.com/...} --- Demo skill \texttt{domain-bva-test}
    end-to-end trên Feature B (FR-09 Mã giảm giá): từ đọc mã nguồn $\to$ phân hoạch miền $\to$
    BVA $\to$ gap analysis $\to$ thực thi API xác nhận bug công thức percent. \emph{(Cần quay
    và dán link trước khi nộp.)}
\end{itemize}

% ============================================================
%  CHƯƠNG 7: TEST SUMMARY REPORT
% ============================================================
\chapter{Test Summary Report}

\begin{table}[H]
    \centering
    \caption{Tổng kết kiểm thử}
    \begin{tabularx}{\linewidth}{L{4.2cm} C{1.9cm} C{1.9cm} C{1.5cm} C{1.5cm} C{1.6cm}}
        \toprule
        \textbf{Tính năng} & \textbf{Thiết kế} & \textbf{Thực thi} & \textbf{Pass} & \textbf{Fail} & \textbf{Note}$^\dagger$ \\
        \midrule
        Feature A (DT)  & 12 & 12 & 3 & 9 & 0 \\
        Feature A (BVA) & 6  & 6  & 4 & 2 & 0 \\
        Feature B (DT)  & 12 & 12 & 6 & 5 & 1 \\
        Feature B (BVA) & 6  & 6  & 5 & 1 & 0 \\
        Feature C (DT)  & 14 & 14 & 6 & 7 & 1 \\
        Feature C (BVA) & 7  & 7  & 3 & 4 & 0 \\
        Feature D (DT)  & 14 & 14 & 4 & 4 & 6 \\
        Feature D (BVA) & 7  & 7  & 3 & 3 & 1 \\
        \midrule
        \textbf{Tổng} & \textbf{78} & \textbf{78} & \textbf{34} & \textbf{35} & \textbf{9} \\
        \bottomrule
    \end{tabularx}
\end{table}
\noindent{\small $^\dagger$ \emph{Note} = ca đã thực thi nhưng kết quả là hành vi ``vùng xám''
(vd âm thầm ép số lượng về 1, phân biệt hoa/thường của mã coupon) --- là khiếm khuyết UX/thiết
kế chứ không phải pass/fail dứt khoát theo đặc tả. Không có ca nào \emph{chưa thực thi} (0/78).}

\begin{table}[H]
    \centering
    \caption{Tổng kết bug}
    \begin{tabularx}{\linewidth}{L{4cm} C{2cm} C{2cm} C{2cm} C{2cm} C{2cm}}
        \toprule
        \textbf{Tính năng} & \textbf{Critical} & \textbf{Major} & \textbf{Minor} & \textbf{Trivial} & \textbf{Tổng} \\
        \midrule
        Feature A & 2 & 4 & 0 & 0 & 6 \\
        Feature B & 1 & 2 & 2 & 0 & 5 \\
        Feature C & 1 & 2 & 2 & 0 & 5 \\
        Feature D & 0 & 2 & 2 & 0 & 4 \\
        \midrule
        \textbf{Tổng} & \textbf{4} & \textbf{10} & \textbf{6} & \textbf{0} & \textbf{20} \\
        \bottomrule
    \end{tabularx}
\end{table}

\begin{passbox}
\textbf{Trạng thái thực thi (2026-07-02).} Cả 78 ca kiểm thử đã được thực thi: Feature A/B/C
gọi HTTP thật tới backend EShop (\texttt{localhost:3000}), Feature D chạy mô phỏng đúng logic
trích từ \texttt{App.js}. \textbf{Toàn bộ 20/20 lỗi ứng viên đều được tái hiện thành công} và
mọi Kết quả thực thi đều \emph{khớp} với dự đoán ban đầu từ phân tích mã nguồn --- không có
dự đoán nào sai. Việc còn lại: chụp màn hình từng bug và mở GitHub Issues tương ứng.
\end{passbox}

% ============================================================
%  PHỤ LỤC A: AI CRITIQUE
% ============================================================
\appendix
\chapter{AI Critique}

Trong suốt bài tập, AI (Claude Code) là một trợ lý mạnh khi \emph{đã được dẫn dắt từng
bước}, nhưng bộc lộ vài điểm mù đáng chú ý. \textbf{Thứ nhất}, AI có thiên hướng dừng ở
\emph{tầng giao diện}: với FR-01 nó phát hiện ngay regex mật khẩu sai, nhưng nếu không được
nhắc ``đọc cả backend'' thì gần như bỏ qua việc \texttt{POST /api/register} không kiểm tra gì
--- cho phép gọi API trực tiếp để né mọi ràng buộc UI. Điểm mù này thuộc về \emph{chất lượng
prompt}: một câu hỏi chỉ nhìn form sẽ nhận câu trả lời chỉ về form.

\textbf{Thứ hai}, AI yếu ở các lỗi thuộc \emph{biến đầu ra} và \emph{phép tính thực tế}. Lỗi
công thức phần trăm của coupon (\texttt{total $\times$ (1$-$discount)}) chỉ lộ ra khi đọc đúng
biểu thức số học, chứ không suy được từ tên biến; tương tự là mật khẩu lưu plaintext bị trả về
qua \texttt{/users/me}. Đây là giới hạn của công cụ: AI mô tả \emph{ý định} của mã tốt hơn
\emph{hệ quả số học} của mã.

\textbf{Thứ ba}, AI bỏ sót các lỗi phụ thuộc \emph{luồng điều khiển} và \emph{trạng thái}: nhánh
bỏ qua giới hạn số lần dùng khi vắng \texttt{user\_id}, hay lỗi giỏ hàng chỉ xuất hiện khi
\texttt{cart.length > 1}. Đây là độ phức tạp nội tại --- cần suy luận tổ hợp, không chỉ liệt kê
đầu vào. Một chi tiết thú vị: lỗi BUG-B-05 (final âm) \emph{không} tái hiện được với coupon
seed vì mọi mã đều có \texttt{discount < min}; phải tự tạo mã qua trang admin mới kích hoạt ---
điều AI ban đầu không lường.

\textbf{Nguyên tắc rút ra:} AI khuếch đại năng suất nhưng \emph{không thay thế} việc con người
đọc mã và \emph{thực thi để xác nhận}. Giá trị của em nằm ở chỗ chia nhỏ kỹ thuật thành từng
bước, ép AI truy vết tới mã nguồn, và cuối cùng chạy thật để biến ``dự đoán'' thành ``bằng
chứng''. ($\approx$270 từ)

% ============================================================
%  PHỤ LỤC B: AI AUDIT REPORT
% ============================================================
\chapter{AI Audit Report}

\section*{Khai Báo Sử Dụng AI}
\textit{``I use AI tools for the following tasks.''}

\section*{Log Các Phiên Làm Việc Với AI}

% ---- Interaction 1 ----
\subsection*{Interaction 1 — Domain Testing \& BVA cho FR-01 (Đăng ký tài khoản)}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3.5cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Công cụ AI    & Claude Code (Opus 4.8) \\
        Ngày giờ      & 2026-07-01 21:57 \\
        Mục đích      & Áp dụng Domain Testing + Boundary Value Analysis cho FR-01, phân tích mã nguồn EShop và thiết kế ca kiểm thử, gap analysis, báo cáo lỗi. \\
        \midrule
        \textbf{Prompt} & \\
        \multicolumn{2}{p{\linewidth}}{%
            \texttt{/domain-bva-test Do requirement 6 (HW02-Domain-Testing.md, Homework-Policies.md, CLAUDE.md)} — kèm hướng dẫn từng bước trong skill \texttt{domain-bva-test} và yêu cầu điền trực tiếp vào \texttt{main.tex} bằng tiếng Việt, chế độ thực thi ``sinh viên tự chạy, AI dự đoán''.
        } \\
        \midrule
        \textbf{AI Output} & \\
        \multicolumn{2}{p{\linewidth}}{%
            AI đọc \texttt{backend/server.js}, \texttt{backend/database.js} và \texttt{frontend-web/src/pages/Register.jsx}; xác định 8 biến (kể cả biến ẩn), phân hoạch miền cho name/email/password (EC-N/E/P), chọn điểm ON/OFF/IN cho biên độ dài mật khẩu, sinh 12 ca Domain (TC-A-DT) + 6 ca BVA (TC-A-BV), bảng AI gap analysis (6 mục), và 6 báo cáo lỗi ứng viên (BUG-A-01..06) suy ra từ mã nguồn. Khiếm khuyết trọng tâm: regex mật khẩu bắt buộc khoảng trắng và cấm ký tự đặc biệt (ngược đặc tả); backend không kiểm tra dữ liệu; email không \texttt{UNIQUE}; mật khẩu lưu plaintext.
        } \\
        \midrule
        \textbf{Human review} & \\
        \multicolumn{2}{p{\linewidth}}{%
            Sinh viên (Lý Quốc Thạnh) chịu trách nhiệm và đã \emph{thực thi} toàn bộ ca trên backend EShop qua HTTP (xem Interaction 5, 2026-07-02): mọi Kết quả/Trạng thái khớp dự đoán từ mã nguồn. Việc còn lại: chụp màn hình và tạo GitHub Issue cho từng bug đã xác nhận.
        } \\
        \bottomrule
    \end{tabularx}
\end{table}

% ---- Interaction 2 ----
\subsection*{Interaction 2 — Domain Testing \& BVA cho FR-09 (Mã giảm giá)}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3.5cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Công cụ AI    & Claude Code (Opus 4.8) \\
        Ngày giờ      & 2026-07-01 22:12 \\
        Mục đích      & Áp dụng Domain Testing + BVA cho FR-09, phân tích logic \texttt{apply-coupon} và thiết kế ca kiểm thử, gap analysis, báo cáo lỗi. \\
        \midrule
        \textbf{Prompt} & \\
        \multicolumn{2}{p{\linewidth}}{%
            \texttt{đọc đề và làm tiếp tục HW02-Domain-Testing.md} — kèm hướng dẫn từng bước trong skill \texttt{domain-bva-test}, điền trực tiếp Chương 4 (Feature B) của \texttt{main.tex} bằng tiếng Việt, cùng style với Feature A, chế độ ``sinh viên tự chạy, AI dự đoán''.
        } \\
        \midrule
        \textbf{AI Output} & \\
        \multicolumn{2}{p{\linewidth}}{%
            AI đọc \texttt{backend/server.js} (\texttt{/api/apply-coupon}) và \texttt{backend/database.js} (bảng \texttt{coupons}, seed SAVE10/BIGBUY/VIP100/EXPIRED); xác định 10 biến (kể cả biến đầu ra), phân hoạch miền cho code/total\_amount/user\_id/type/expiry, chọn điểm ON/OFF/IN cho biên \texttt{min\_order\_amount}, sinh 12 ca Domain (TC-B-DT) + 6 ca BVA (TC-B-BV), bảng gap analysis (6 mục), và 5 báo cáo lỗi ứng viên (BUG-B-01..05). Khiếm khuyết trọng tâm: công thức percent \texttt{1$-$discount\_value} làm giá tăng $\sim$10 lần; biên \texttt{min\_order\_amount} dùng \texttt{>} (off-by-one); bỏ qua giới hạn dùng khi vắng \texttt{user\_id}; endpoint không xác thực; không kẹp \texttt{final\_amount} $\ge 0$.
        } \\
        \midrule
        \textbf{Human review} & \\
        \multicolumn{2}{p{\linewidth}}{%
            Sinh viên (Lý Quốc Thạnh) chịu trách nhiệm và đã \emph{thực thi} toàn bộ ca trên backend EShop qua HTTP (xem Interaction 5, 2026-07-02): mọi Kết quả/Trạng thái khớp dự đoán từ mã nguồn. Việc còn lại: chụp màn hình và tạo GitHub Issue cho từng bug đã xác nhận.
        } \\
        \bottomrule
    \end{tabularx}
\end{table}

% ---- Interaction 3 ----
\subsection*{Interaction 3 — Domain Testing \& BVA cho FR-14 (Quản lý danh mục)}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3.5cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Công cụ AI    & Claude Code (Opus 4.8) \\
        Ngày giờ      & 2026-07-01 22:20 \\
        Mục đích      & Áp dụng Domain Testing + BVA cho FR-14 (CRUD danh mục, trang admin); phân tích 4 endpoint category + middleware xác thực. \\
        \midrule
        \textbf{Prompt} & \\
        \multicolumn{2}{p{\linewidth}}{%
            \texttt{tiếp tục testing, phần feature B có dùng /domain-bva-test không, nếu không thì dùng rồi kiểm tra lại, làm phần c} — dùng skill \texttt{domain-bva-test}, điền trực tiếp Chương Feature C của \texttt{main.tex} bằng tiếng Việt, cùng style Feature A/B, chế độ ``sinh viên tự chạy, AI dự đoán''.
        } \\
        \midrule
        \textbf{AI Output} & \\
        \multicolumn{2}{p{\linewidth}}{%
            AI đọc \texttt{backend/server.js} (4 endpoint \texttt{/api/categories} + middleware \texttt{authenticateToken}) và \texttt{backend/database.js} (bảng \texttt{categories}, seed 3 danh mục + sản phẩm); xác định 7 biến, phân hoạch miền cho name/id/quyền, chọn điểm ON/OFF/IN cho ranh giới \emph{tồn tại} của \texttt{id}, sinh 14 ca Domain (TC-C-DT) + 7 ca BVA (TC-C-BV), bảng gap analysis (6 mục), và 5 báo cáo lỗi ứng viên (BUG-C-01..05). Khiếm khuyết trọng tâm: thiếu kiểm soát quyền admin (mọi user CRUD được danh mục); không kiểm \texttt{name} (rỗng/trùng/dài); PUT/DELETE id không tồn tại vẫn trả 200; xóa danh mục làm mồ côi sản phẩm (thiếu khóa ngoại).
        } \\
        \midrule
        \textbf{Human review} & \\
        \multicolumn{2}{p{\linewidth}}{%
            Sinh viên (Lý Quốc Thạnh) chịu trách nhiệm và đã \emph{thực thi} toàn bộ ca trên backend EShop qua HTTP (xem Interaction 5, 2026-07-02): mọi Kết quả/Trạng thái khớp dự đoán từ mã nguồn. Việc còn lại: chụp màn hình và tạo GitHub Issue cho từng bug đã xác nhận.
        } \\
        \bottomrule
    \end{tabularx}
\end{table}

% ---- Interaction 4 ----
\subsection*{Interaction 4 — Domain Testing \& BVA cho FR-07 (Giỏ hàng — Mobile)}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3.5cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Công cụ AI    & Claude Code (Opus 4.8) \\
        Ngày giờ      & 2026-07-01 22:28 \\
        Mục đích      & Áp dụng Domain Testing + BVA cho FR-07 (giỏ hàng app mobile React Native/Expo); phân tích logic giỏ trong \texttt{frontend-mobile/App.js}. \\
        \midrule
        \textbf{Prompt} & \\
        \multicolumn{2}{p{\linewidth}}{%
            \texttt{tiếp tục /domain-bva-test} — dùng skill \texttt{domain-bva-test} cho Feature D (mobile), điền trực tiếp chương Feature D của \texttt{main.tex} bằng tiếng Việt, cùng style Feature A/B/C, chế độ ``sinh viên tự chạy, AI dự đoán''.
        } \\
        \midrule
        \textbf{AI Output} & \\
        \multicolumn{2}{p{\linewidth}}{%
            AI đọc \texttt{frontend-mobile/App.js} (\texttt{normalizeQuantity}, \texttt{addToCart}, ô sửa số lượng trong giỏ, \texttt{cartTotal}, \texttt{handleConfirmCheckout}); xác định 6 biến (gồm biến đầu ra \texttt{total\_amount}), phân hoạch miền cho quantity/ô sửa/cart.length, chọn điểm ON/OFF/IN cho biên \texttt{quantity}$>0$ và \texttt{cart.length}=1, sinh 14 ca Domain (TC-D-DT) + 7 ca BVA (TC-D-BV), gap analysis (5 mục), và 4 báo cáo lỗi ứng viên (BUG-D-01..04). Khiếm khuyết trọng tâm: ô sửa số lượng lưu \texttt{parsed+1} (off-by-one); checkout \texttt{slice(0,-1)} bỏ mất món cuối nhưng vẫn tính đủ tiền; ép âm thầm số lượng không hợp lệ về 1; không giới hạn số lượng/tồn kho.
        } \\
        \midrule
        \textbf{Human review} & \\
        \multicolumn{2}{p{\linewidth}}{%
            Sinh viên (Lý Quốc Thạnh) chịu trách nhiệm và đã \emph{thực thi} logic giỏ hàng bằng mô phỏng đúng mã \texttt{App.js} (xem Interaction 5, 2026-07-02): kết quả khớp dự đoán. Việc còn lại: chụp màn hình trên app Expo và tạo GitHub Issue.
        } \\
        \bottomrule
    \end{tabularx}
\end{table}

% ---- Interaction 5 ----
\subsection*{Interaction 5 — Thực thi \& xác nhận toàn bộ ca kiểm thử (FR-01/09/14/07)}
\begin{table}[H]
    \centering
    \begin{tabularx}{\linewidth}{L{3.5cm} X}
        \toprule
        \textbf{Trường} & \textbf{Nội dung} \\
        \midrule
        Công cụ AI    & Claude Code (Opus 4.8) \\
        Ngày giờ      & 2026-07-02 \\
        Mục đích      & \emph{Thực thi thật} toàn bộ ca kiểm thử đã thiết kế (Interaction 1--4) trên EShop để chuyển cột Kết quả/Trạng thái từ ``dự đoán'' thành kết quả chạy thực, và xác nhận 20 lỗi ứng viên. \\
        \midrule
        \textbf{Prompt} & \\
        \multicolumn{2}{p{\linewidth}}{%
            \texttt{tiếp tục hoàn thành các feature bên dưới, đọc qua tất cả md trong codebase và sử dụng skill khi cần} --- AI đọc lại đề + skill, khởi chạy backend, thực thi ca kiểm thử qua HTTP và cập nhật \texttt{main.tex}.
        } \\
        \midrule
        \textbf{AI Output} & \\
        \multicolumn{2}{p{\linewidth}}{%
            AI seed lại DB (\texttt{node database.js}), chạy backend (\texttt{node server.js}, \texttt{localhost:3000}), rồi thực thi: (i) FR-01 --- \texttt{POST /api/register} với email sai định dạng/trùng/body rỗng/mật khẩu yếu, \texttt{GET /api/users/me} lộ mật khẩu plaintext, và chạy đúng regex trích từ \texttt{Register.jsx}; (ii) FR-09 --- \texttt{POST /api/apply-coupon} với SAVE10 (final=4.000.000, discount=$-3.600.000$), VIP100 tại biên 300.000 (bị từ chối), bypass khi vắng \texttt{user\_id}, không xác thực token, và mã fixed tự tạo cho final âm ($-40.000$); (iii) FR-14 --- 4 endpoint \texttt{/api/categories} với token admin lẫn user (tạo được bằng token user $\Rightarrow$ thiếu phân quyền), PUT/DELETE id không tồn tại vẫn 200, xóa danh mục làm mồ côi 2 sản phẩm; (iv) FR-07 --- mô phỏng \texttt{normalizeQuantity}, ô sửa (\texttt{parsed+1}), \texttt{slice(0,-1)} trích từ \texttt{App.js}. \textbf{Kết quả: 78/78 ca thực thi, 20/20 lỗi tái hiện, mọi kết quả khớp dự đoán.} Sau đó AI điền số liệu Test Summary, mô tả Agent Skill, và AI Critique.
        } \\
        \midrule
        \textbf{Human review} & \\
        \multicolumn{2}{p{\linewidth}}{%
            Sinh viên (Lý Quốc Thạnh) chịu trách nhiệm: đối chiếu output HTTP thực tế với cột Kết quả/Trạng thái; xác nhận không lỗi nào bị bịa. Riêng BUG-B-05 cần mã coupon \texttt{discount > min} (không có trong seed) nên được tạo qua trang admin --- đã ghi rõ trong ca TC-B-DT-12. Việc còn lại thuộc về sinh viên: chụp màn hình từng bug và mở GitHub Issues.
        } \\
        \bottomrule
    \end{tabularx}
\end{table}

% Thêm các interaction tiếp theo theo cùng cấu trúc

% ============================================================
%  PHỤ LỤC C: GIT COMMIT LOG
% ============================================================
\chapter{Git Commit Log}

\begin{lstlisting}[language=bash, caption={Git commit log}]
# TODO: Paste output của lệnh:
# git log --oneline --all
# hoặc git log --pretty=format:"%h %ad %s" --date=short

[hash] [date] feat(feature-a): domain testing - identify input variables
[hash] [date] feat(feature-a): domain testing - partitioning
[hash] [date] feat(feature-a): domain testing - test cases
[hash] [date] feat(feature-a): bva - boundary identification
[hash] [date] feat(feature-a): bva - test cases
[hash] [date] bug(feature-a): report BUG-A-01
...
\end{lstlisting}

% ============================================================
%  TÀI LIỆU THAM KHẢO
% ============================================================
\chapter*{Tài Liệu Tham Khảo}
\addcontentsline{toc}{chapter}{Tài Liệu Tham Khảo}

\begin{enumerate}
    \item ISTQB Foundation Level Syllabus (latest edition).
    \item Hardman, P. (2025). \textit{A Post-AI Learning Taxonomy}.
    \item Fuster Rabella, M. (2025). \textit{OECD Education Working Paper No.~338}.
    \item Anthropic (2025). \textit{Building Reliable AI Test Agents} — engineering blog.
    \item DeepEval \& Promptfoo documentation — LLM testing frameworks.
\end{enumerate}

\end{document}