# Git commit log — HW02 Domain Testing
# Để trống — tự điền bằng lệnh (chạy tại thư mục repo, sau khi đã commit hết các bước):
#   git log --pretty=format:"%h | %ad | %s" --date=format:"%Y-%m-%d %H:%M" > reports/git-commit-log.txt
#
# (Xóa các dòng ghi chú này sau khi dán log thật vào.)
